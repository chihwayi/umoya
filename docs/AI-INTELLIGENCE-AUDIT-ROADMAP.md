# AI/Intelligence Subsystem Audit & Remediation Roadmap (S262+)

**Created:** 2026-08-26. Follow-up to the S246–S261 system-wide audit (see memory `project_audit_remediation_s246_s261` — that roadmap doc was deleted once fully resolved). This one covers a deep audit of the CDSS, PostVisit AI companion, Voice/Ambient/Copilot, and AI recommendation/ingestion subsystems specifically, run via 4 parallel deep-dive investigations.

**Golden rule carried over unchanged:** any per-tenant database schema change goes through `getProvisioningBundles()` in `services/tenant-service/src/services/database-provisioning.service.ts` — never a raw migration file. Read `docs/PROVISIONING_GUIDE.md` before touching schema. System-DB changes go in `TenantService.ensureSubscriptionSchema()`. Always run system-provisioning-then-tenant-repair in that order.

**Second rule, learned the hard way in the prior engagement:** `services/tenant-service/src/generated/tenant-entity-alignment.statements.ts` is a live, executed provisioning bundle — never `git checkout --` it to discard an unwanted diff. Regenerate (`npm run generate:tenant-provisioning-alignment`) and bump `BUNDLE_VERSION` instead.

---

## Part 1 — Findings (F1–F22)

Found via 4 parallel Explore-agent deep-dives: CDSS Python service, PostVisit AI companion pipeline, Voice/Ambient/Encounter-Copilot, and AI recommendation/ingestion. Each finding was verified against real callers (not just read in isolation) before being listed here.

### 1.1 Tier 1 — Critical (patient safety / compliance / silent data loss)

| # | Finding | Location | Severity |
|---|---|---|---|
| F1 | **RESOLVED in S262 (2026-08-26).** `clinical_safety.apply_safety_governor()` is shaped specifically for the readmission-risk payload (`risk_level`/`overall_score`/`readmission_assessment`), so rather than force-fit it onto 3 differently-shaped responses, added two new pure helper functions (`diagnosis_acute_check`, `discharge_acute_check`) that reuse the same underlying `evaluate()` deterministic check without assuming those fields exist: `/diagnosis/suggest` and `/diagnosis/suggest/intelligent` (including its service-unavailable fallback path, so the check still runs even when the AI model itself is down) prepend an "ACUTE DETERIORATION DETECTED" red flag distinct from the existing AI-governance `safety_gate`; `/discharge/intelligence` adds `discharge_safe: false` and prepends a "do not discharge" intervention. Also fixed a real data-flow bug found while wiring this: `clinical_safety.extract_vitals()` didn't recognise the `hr` key `/discharge/intelligence`'s own request model uses for heart rate (only `heartRate`/`pulse`/`heart_rate` were aliased) — added `hr` to the alias list. **CI lesson learned the hard way**: the first version of the new test file imported `main.py` via FastAPI `TestClient`, which pulled in `main.py`'s module-level `torch`/`transformers`/`chromadb`/`boto3` imports — the CI safety-gate step deliberately only installs bare `pytest` (installing the full ML stack would make every PR wait minutes on torch), so that first version passed locally (full venv) but failed in CI (`ModuleNotFoundError: fastapi`), caught from the real CI run's error output. Fixed by keeping the new safety-check logic in the two pure helper functions above (zero heavy deps, `clinical_safety.py` already has none) and rewriting the test to import only `clinical_safety`, not `main` — verified by running the exact CI command in a scratch venv with nothing but `pytest` installed. New test file `test_diagnosis_discharge_safety_governor.py` (4 tests) added to the CI safety-gate list in `ci.yml`. Verified: 26/26 tests pass in a bare-`pytest`-only venv (0.05s — matches CI's actual environment, not just local dev). | `services/cdss-service/main.py:4300-4337,4440-4520,14671-14780`, `clinical_safety.py:extract_vitals,diagnosis_acute_check,discharge_acute_check`, `tests/test_diagnosis_discharge_safety_governor.py` | RESOLVED |
| F2 | **RESOLVED in S263 (2026-08-26).** Replaced the keyword-matching draft-note updater with a real LLM call via the existing `call_governed_json()`/`LLMProvider` infrastructure already used elsewhere in this file (e.g. `/cdss/cultural/sdoh-risk`) — the prompt passes the existing draft note plus the new transcript fragment and asks for updated SOAP fields, explicitly instructed not to duplicate content across sections or infer unsaid findings. On LLM failure, fields are left unchanged rather than falling back to keyword-guessing — an empty/unchanged field is honest, a keyword-guessed one looks real but isn't. **Also found and fixed a second, more severe bug in the same endpoint while implementing this**: the entity-extraction call just above it invoked `diagnostic_assistant.suggest_diagnoses` (plural) with `context=` — neither the method name nor the `context` kwarg exist on `DiagnosticAssistant` (only `suggest_diagnosis`, singular, taking `symptoms: List[str]`), so entity extraction has been throwing `AttributeError` on literally every call since this endpoint shipped, silently caught by the surrounding `except Exception`. Fixed to call the real method with the real signature and correct return-shape mapping (`diagnosis`/`icd10`/`probability`, not the nonexistent `name`/`icd_code`/`confidence`) — verified against real consumers (`AmbientBar.tsx`, `voice-transcription.service.ts`, `ambient.service.ts`) which all read `d.text`/`d.icd`, matching the fixed output shape. Verified: full CI safety-gate pytest set (26 tests, run in a bare-`pytest`-only venv matching the real CI environment exactly) passes; CDSS release-gate suite (`evaluation/run_release_gate_suite.py`) passes (`Blocked: False`). **Not independently verified against a live audio session** (would require the full Whisper/voice_scribe stack) — the fix is verified at the syntax/logic/shape-compatibility level, same class of limitation as the mobile e2e specs elsewhere in this engagement that need a real device. | `services/cdss-service/main.py:4968-5040` | RESOLVED |
| F3 | **RESOLVED in S264 (2026-08-27).** The blanket `.catch(() => {})` now logs the failure at error level with session/severity context via a new `logger` on `PostVisitService` (the class had zero logging infrastructure anywhere — added `private readonly logger = new Logger(PostVisitService.name)`). Deliberately still not rethrown — the patient must still get their answer even if the secondary nurse-task/alert routing fails — but the failure is no longer invisible. | `services/ehr-service/src/services/post-visit.service.ts:6898-6903` | RESOLVED |
| F4 | **RESOLVED in S265 (2026-08-27) — root cause turned out to be different from the `care_gaps` precedent, not the same bug.** Investigated first, per the roadmap's instruction: unlike `care_gaps` (3 `CREATE TABLE` statements racing for one table name, one of them fully dead code), `post_visit_escalations` and `post_visit_escalation_events` are two separately-named tables with zero naming collision — not a race. Tracing every call site showed they're written **together, from the same trigger** (a patient-message escalation in `post-visit.service.ts`'s companion-chat handler) and hold genuinely different data: `post_visit_escalation_events` is the primary, deeply-integrated system (SLA due-dates, `route_target`, acknowledgement lifecycle, `workflow_key` linkage into the durable workflow engine from S254, queried by 20+ call sites across dashboards/reports) while `post_visit_escalations` (via `PostVisitEscalationRoutingService`) exists specifically to create the actionable `nurse_tasks` row the nurse worklist depends on, plus the on-call-aware real-time alert delivery hardened in S264. Neither is dead code and neither should absorb the other — **the real bug was the missing link between two legitimately-separate records for one event**, not a collision. Fix: added `post_visit_escalation_events.routing_escalation_id` (new column, `post_visit_escalations`/nurse_tasks bundle golden-rule change, version bumped), populated it at the single call site right after both records are created, and joined it into `PostVisitEscalationService.listEscalations()` — the actual dashboard read path — surfacing `routingEscalationId`/`routingNurseTaskId`/`routingStatus`/`routingAlertDeliveryFailed` on every escalation row so a dashboard reading the primary table can now tell whether a nurse task exists and whether its alert delivery succeeded, without needing to separately know about or query the second table. New tests: 2 in `post-visit-escalation.service.spec.ts` (linked record surfaced; absent record leaves fields null). Verified: 75 tests pass across the 3 touched spec files; `audit:tenant-provisioning`/`audit:tenant-fk-drift` clean; `tsc --noEmit` clean in `ehr-service` and `tenant-service`. | `services/ehr-service/src/services/post-visit.service.ts:6892-6915`, `post-visit-escalation.service.ts:listEscalations,mapEscalationEvent`, `services/tenant-service/src/services/database-provisioning.service.ts` | RESOLVED |
| F5 | **RESOLVED in S264 (2026-08-27).** `alertDelivery` is no longer `@Optional()` — confirmed both it and `PostVisitEscalationRoutingService` are always registered in `ehr.module.ts`, so the optionality was never a real runtime scenario, just a way for a wiring mistake to fail silently instead of at startup. Extracted a new `deliverAlert()` private method with its own try/catch around `broadcastCriticalAlert()`. Also found and fixed a second failure mode in the same call while wiring this: `broadcastCriticalAlert()` previously returned `void` even when it found zero on-call staff to notify (`if (!staff.length) { ...warn...; return; }`) — silently doing nothing with no signal to the caller. Changed its return type to `{ delivered: boolean; recipientCount: number }` (additive change — the 11 other real callers all just `await` it without using the return value, so none needed updating; the one that does return it, `alert-delivery.controller.ts`'s manual `/alerts/broadcast` endpoint, now usefully reports delivery outcome instead of an empty body). On any delivery failure (thrown error or zero recipients), the escalation row is updated with new `alert_delivery_failed`/`alert_delivery_error` columns (`post_visit_escalations` bundle, version bumped) rather than the failure vanishing — the nurse task and escalation record still stand (not rethrown/undone) since a nurse can still find them by working the open-escalations list even without real-time delivery. 2 new tests cover both failure paths (throw, and zero-recipients). Verified: `post-visit-escalation-routing.spec.ts` (7 tests) and `post-visit.service.spec.ts` (49 tests) both pass; `audit:tenant-provisioning` and `audit:tenant-fk-drift` clean; `tsc --noEmit` clean in `ehr-service` and `tenant-service`. | `services/ehr-service/src/services/post-visit-escalation-routing.service.ts`, `services/ehr-service/src/services/alert-delivery.service.ts`, `services/tenant-service/src/services/database-provisioning.service.ts` | RESOLVED |
| F6 | **RESOLVED in S266 (2026-08-28).** `getAllActiveTenants()`/`getTenantDatabase()` failures now `logger.error()` with full context (which tenant, which error) instead of vanishing into an empty array/null — the single most severe failure mode (a tenant-service outage previously meant zero tenants processed with zero trace). Added `detectStuckSessions()`: flags sessions still in `'captured'` status after 15 minutes (5+ missed cron cycles), logged at error level with the stuck session IDs per tenant. Per the roadmap table's own "No DB provisioning touch" call, this is log-based operator visibility, not a new tracking table — no ops-alerting infrastructure exists in this codebase yet to build on. 4 new tests (2 failure-surfacing, 2 stuck-detection); existing 5 tests updated so the new 3rd query call (stuck-session detection) doesn't produce spurious noise. | `services/ehr-service/src/services/post-visit-ingestion-cron.service.ts` | RESOLVED |
| F7 | **RESOLVED in S267 (2026-08-28).** Wired `recordExecution()` into every **genuine execution point** (not every `buildSurfaceMetadata` call site — several turned out to be legitimate read/decoration-time formatting of already-executed AI output, converting those would have spammed the audit log on every chart view/mobile poll): `radiology-ai.service.ts` (`analyzeStudy`/`analyseStudyWithDb`), all 3 sites in `claims-ai.service.ts` (denial prediction, appeal generation, PDMP check), `encounter-copilot.service.ts` (`generateSession`), `post-visit-grounded-llm.service.ts` (all 6 LLM call sites — this service had no `tenantDb` access at all, so added `@Optional() TenantService` and resolve `tenantDb` from `tenantId` internally, falling back to unpersisted display metadata when neither is available), `registration-intelligence.service.ts` (`extractRegistrationDocument`, both the non-persist and persist paths). **Investigated before touching, per the roadmap's stated approach**: `patient-ai.service.ts` already had its own real, working `HipaaAuditService`-backed audit path (`recordPatientAiPromptAudit`) — not actually part of this gap, left alone. `oncology.service.ts`'s `getMobileProtocolSnapshot` and `patient-intelligence.service.ts`'s `getPatientIntelligence` are read/decoration-time only (mobile polling, chart-view aggregation of already-audited sub-system outputs) — correctly left as display-only `buildSurfaceMetadata` calls. `recordExecution`'s missing-dependency fallback now `logger.warn()`s instead of failing invisibly. New `npm run audit:ai-surface-execution-wiring` CI gate (`scripts/audit-ai-surface-execution-wiring.mjs`, wired into `.github/workflows/ci.yml`) fails the build if a service calls `buildSurfaceMetadata` without ever calling `recordExecution` or writing its own real audit trail, unless explicitly allowlisted with a stated reason — matches the pattern of `audit-controller-guards.mjs`. | `services/ehr-service/src/services/ai-surface-contract.service.ts`, `radiology-ai.service.ts`, `claims-ai.service.ts`, `encounter-copilot.service.ts`, `post-visit-grounded-llm.service.ts`, `registration-intelligence.service.ts` | RESOLVED |

### 1.2 Tier 2 — High (real gaps, silent failures, not immediately life-threatening)

| # | Finding | Location | Severity |
|---|---|---|---|
| F8 | **RESOLVED in S268 (2026-08-28).** Added a `grounding_status`/`grounding_reason` field pair to every `intelligent_suggest()` return path: `'grounded'` (retrieved chunks were included in the prompt), `'ungrounded_no_results'` (retrieval ran but found nothing), `'ungrounded_retrieval_failed'` (retrieval threw), or `'not_attempted'` (no knowledge_registry/RAG engine configured, or the LLM path was never reached). **Also found and fixed a real adjacent bug while implementing this**: citations were only attached to the response when `self.rag_engine` was truthy, even when the retrieved docs came entirely from `knowledge_registry` — a knowledge-registry-only-grounded answer silently lost its citations and would have incorrectly failed the governance gate's `require_citations`/`min_citation_count` check (`ai_governance.py`'s `apply_safety_gate`). Fixed to check `retrieved_docs` truthy directly. 5 new tests (`test_diagnostic_assistant_grounding_status.py`, added to the CI safety-gate list) cover all four grounding states plus the citation-attachment fix; imports only `diagnostic_assistant.py` (no heavy deps at module level), verified passing in the bare-pytest CI-simulation venv. | `services/cdss-service/diagnostic_assistant.py:943-981,1030` | RESOLVED |
| F9 | **RESOLVED in S268 (2026-08-28) — investigated first, per the roadmap's own "delete vs. build" framing.** `guidelines_engine` (the class with the empty `GUIDELINES` dict) turned out NOT to be dead code — it's wired as `ClinicalKnowledgeRegistry`'s `fallback_engine` in `main.py:1633` and is genuinely invoked (`clinical_knowledge_registry.py:276`) whenever both the governed registry search AND RAG retrieval find nothing. Its `check_guidelines()` logic already anticipated exactly 5 conditions (age/gender/comorbidity/medication-based recommendation and contraindication logic keyed on `hypertension`/`diabetes_type2`/`pneumonia`/`uti`/`heart_failure`) that were simply never populated. Populated `GUIDELINES` with conservative, well-established primary-care guidance for those 5 conditions (general management principles only — deliberately excludes precise dosing/titration, which belongs in the governed, versioned registry, not a hardcoded last-resort fallback) sourced to WHO/general primary-care standards. Verified: `test_clinical_knowledge_registry.py`'s 15 tests (which construct the registry without a `fallback_engine`, so unaffected) still pass unchanged; the empty-guideline behavior (`evidence_level: 'unknown'`, honest "no guideline found" message) was already correct and is preserved for conditions outside the covered 5. | `services/cdss-service/clinical_guidelines.py:14,68-79` | RESOLVED |
| F10 | CDSS job-worker loop's exception handling is a broad `except Exception:` that waits and retries with no logging — Redis connection loss, embedding failures, or vector-store unavailability produce an infinite silent retry loop with zero operator visibility. | `services/cdss-service/main.py:2040-2041` | HIGH |
| F11 | **RESOLVED in S269 (2026-08-28).** `routeEscalationToWorkflow` now wraps the INSERT in try/catch, logs at error level on failure, and returns `null` instead of the workflowKey — both existing call sites already had `if (workflowKey) { UPDATE ... }` guards, so returning `null` on failure alone closes the ghost-workflow gap with no caller changes needed. 2 new tests. | `services/ehr-service/src/services/post-visit.service.ts:10775-10807` | RESOLVED |
| F12 | **RESOLVED in S271 (2026-08-28).** `classifyEscalationSignals` now routes through `PostVisitGroundedLlmService.classifyEscalationSignal()`, using the (retained, renamed to `classifyEscalationSignalsKeywordFallback`) keyword matcher's output as the `stage1_prefilter` (`triggerTerms`/`candidateSeverity`) the LLM prompt was already designed to accept but never received. Falls back to pure keyword matching, unchanged, whenever `groundedLlmService` is unavailable, the message is empty, or the LLM call throws (logged at warn level). Discovered while implementing: an **existing** test (`classifies historical high-risk message as suppressed...`) already mocked `classifyEscalationSignal` on `groundedLlmServiceMock` and passed before this fix — but only because its input happened to also satisfy the keyword path identically, silently leaving the LLM mock dead/unexercised the whole time. Extended that test with an explicit assertion that the mock is actually called and that `classifierSource: 'llm_v1'` is returned. | `services/ehr-service/src/services/post-visit.service.ts:10718-10749` | RESOLVED |
| F13 | Voice session IDs use `Math.random()` (`voice_${Date.now()}_${Math.random().toString(36).slice(2,8)}`) — same weak-ID class already fixed elsewhere in the codebase (MRNs, claim numbers) via `crypto.randomInt`/`randomBytes`, but this one was missed. | `services/ehr-service/src/services/voice-transcription.service.ts:44` | HIGH |
| F14 | **RESOLVED in S269 (2026-08-28).** `generateSession()` now catches the order-pipeline save failure, logs at error level, and writes `orderPipelineSaved: false` + `orderPipelineSaveError` onto the session's existing `governance` JSONB column (no new schema needed) — a dashboard/UI reading the session can now tell suggested orders were generated but never actually reached the pipeline a clinician approves/rejects from. 1 new test. | `services/ehr-service/src/services/encounter-copilot.service.ts:215` | RESOLVED |
| F15 | **RESOLVED in S269 (2026-08-28) — the "nightly sweep" claim was verified FALSE, and a much deeper bug was found underneath it.** The only cron in `FollowUpRecommendationService` (`sweepOverdueFollowUps`, 7am daily) alerts on already-existing overdue `followup_recommendations` rows — it has zero mechanism to backfill a recommendation that was never created because `generateRecommendation()` threw. Worse: that whole service's schema/API (`followup_recommendations.patient_id INTEGER`, `parseInt()` at every controller read) expects **integer** patient IDs, fundamentally incompatible with this codebase's UUID `patients` table — the call site did `patientId: Number(body.patientId) || 0`, meaning every "successful" call was silently inserting a **corrupted `patient_id=0` row**, not a real per-patient recommendation. Also confirmed zero frontend caller anywhere in `ehr-frontend`. Rather than building a bigger sweep on top of broken data, removed the fire-and-forget call and its now-unused `FollowUpRecommendationService` injection from `post-visit.controller.ts` — a real fix needs a UUID migration of the entire service as its own dedicated piece of work (flagged, not attempted as a side effect of a silent-catch sweep). `followup.controller.ts`'s own standalone endpoints (also integer-ID-based, also with no frontend caller) were left untouched — out of scope, a separate pre-existing issue, not something this sprint's fire-and-forget call was the only user of. | `services/ehr-service/src/controllers/post-visit.controller.ts:80-106` | RESOLVED |
| F16 | **RESOLVED in S270 (2026-08-28).** Removed all 5 `.catch(() => {})`/`.catch(() => [])` swallows in `ai-performance.service.ts` (`requestModelReview`, `updateModelStatus`, `getGovernanceHistory`, `getCalibrationPlot`, `getModelFairness`'s two parallel queries) so DB failures propagate as real exceptions. Verified this is the right fix, not just "throw and hope": the global `AllExceptionsFilter` converts any uncaught error into a proper `{message, statusCode}` JSON response (Sentry-reported too), and `AiGovernanceDashboard.tsx` **already has** `ErrorBanner`/retry UI wired to `err?.response?.data?.message` for every one of these calls — it was simply dead code because the backend never actually threw. 6 new tests (`ai-performance.service.spec.ts`, first coverage for this file) cover all 5 propagation paths plus the fairness happy-path. | `services/ehr-service/src/services/ai-performance.service.ts:178,194,205,225,251` | RESOLVED |
| F17 | Knowledge-document ingestion is fire-and-forget — a document is returned with `ingestionStatus: 'pending'` immediately, and if the async ingestion job then fails, only a log line records it. The document stays `'pending'` forever from the user's perspective. | `services/ehr-service/src/services/knowledge-ingest.service.ts:52-54` | HIGH |
| F18 | **RESOLVED in S269 (2026-08-28).** `persistAudit(...)` failures are still fire-and-forget (correct — the caller must not block on audit writes) but now `logger.error()` with recommendation type and patient ID instead of vanishing via `.catch(() => {})`. 2 new tests (`ai-explainability.service.spec.ts`, first coverage for this file). | `services/ehr-service/src/services/ai-explainability.service.ts:44` | RESOLVED |

### 1.3 Tier 3 — Medium/Low (hygiene, compliance nuance, low blast radius)

| # | Finding | Location | Severity |
|---|---|---|---|
| F19 | HIPAA PHI-access audit calls throughout `post-visit.service.ts` are consistently `.catch(() => {})` — if audit logging fails, PHI access proceeds unrecorded and nothing surfaces that. Many call sites (≈8+), same pattern each time. | `services/ehr-service/src/services/post-visit.service.ts` (multiple: 1389, 1555, 1697, 1916, 1933, 1965, 2275, ...) | MEDIUM |
| F20 | Companion-memory retrieval failures aren't surfaced — if `getPatientCompanionMemoryFacts()` fails, the LLM still answers, just without memory context, while the surrounding code's "grounded" framing implies it always has context. | `services/ehr-service/src/services/post-visit.service.ts:6853-6859` | MEDIUM |
| F21 | `AiPerformanceService.recordPrediction()` accepts any DTO with no schema validation — any internal service can write arbitrary confidence scores/model versions/unvalidated JSON `input_features`, corrupting the model-performance tracking data other dashboards trust. | `services/ehr-service/src/services/ai-performance.service.ts:8-25` | MEDIUM |
| F22 | Ambient audio-chunk processing (`processChunk`) is only reachable via the WebSocket gateway — `AmbientController` has START/PAUSE/RESUME/END/ACTION REST endpoints but no chunk/process endpoint. Currently fine since the frontend only uses WebSocket, but it's an asymmetric API surface that would silently be a dead end for any REST-only client. | `services/ehr-service/src/services/ambient.service.ts:119`, `ambient.controller.ts` (missing) | LOW |

### 1.4 Confirmed fine — do not re-audit

- **CDSS**: clinical safety governor itself (pure functions, correct escalation logic), risk-scoring formulas (Framingham, readmission, adherence — real clinical math), dosing calculator / duplicate-therapy checker / drug-interaction logic (real rule-driven, not hardcoded examples), NICU jaundice calculator (correct Bhutani nomogram with preterm adjustment), AI-governance safety-gate confidence/citation checks, acute job-failure handling (dead-letter queue works correctly), knowledge-ingest validation script (`ingest_guidelines.py`, checks RAG init + PDF existence before proceeding).
- **PostVisit**: grounded-LLM service's citation validation (rejects ungrounded output, has a circuit breaker, logs real errors — does NOT silently fake grounding), ambient-to-postvisit handoff (errors caught and logged, doesn't break the calling flow), telemedicine-postvisit bridge (async recording fetch doesn't block the response, errors logged with context).
- **Voice/Ambient**: the two transcription services are NOT duplicates — `TranscriptionService` (file-upload Whisper) and `VoiceTranscriptionService` (real-time CDSS with abstention logging) serve genuinely different purposes with no overlap. All voice/ambient/copilot SQL queries target real, correctly-provisioned tables. The retry-and-requeue pattern in `ambient.service.ts:59-87` is intentional, not a silent failure.
- **AI recommendation**: `followup-recommendation.service.ts` is fully implemented, not stubbed, with a real rule-based fallback on LLM failure. `ai-order-pipeline.service.ts` core save/approve/reject cycle is complete. `outcome-linkage.service.ts` is properly implemented. **The retraining/governance-honesty fix from the prior engagement (S253) is still correctly in place and has not regressed** — `outcome-collection.service.ts` still honestly reports `status: 'feedback_queued'`/`deploymentMethod: 'manual_review_required'` rather than claiming a fake auto-deployment.

---

## Part 2 — Sprint Plan (S262+)

Ordered by severity tier, same as the prior engagement. Tier 1 sprints should land before Tier 2/3 — several share root causes with each other (F3/F5/F7 are all "silent catch swallows a compliance/safety-critical failure").

### TIER 1 — Critical

| Sprint | Title | What It Delivers | DB provisioning touch? |
|---|---|---|---|
| **S262** | CDSS Safety Governor Universal Enforcement | Wire `apply_safety_governor()` into `/diagnosis/suggest`, `/diagnosis/suggest/intelligent`, and `/discharge/intelligence` (F1). Each response gets a real acute-deterioration flag distinct from the existing AI-governance `safety_gate` field, so the two can't be confused. Add regression tests asserting a hypoxic/tachycardic vitals payload triggers the flag on all 3 endpoints. | No |
| **S263** | Real LLM SOAP Note Generation | Replace the keyword-matching draft-note updater in ambient transcription (F2) with a real LLM call, matching the pattern already used elsewhere in the CDSS service for structured clinical output (citations, confidence). Add a fallback that clearly marks output as "unavailable" rather than degrading to keyword-matching silently. | No |
| **S264** | PostVisit Escalation Delivery Reliability | Fix the blanket swallow at F3 — surface routing failures (retry queue or explicit error state visible to the session), remove `@Optional()` from `alertDelivery` or make its absence a hard startup failure instead of a silent capability loss (F5), add error handling inside `routeEscalation()` itself instead of relying on the caller's catch-all. | No |
| **S265** | PostVisit Escalation Schema Consolidation | Investigate F4's root cause first — check whether it's the same "dead bundle wins the race" pattern as `care_gaps`. Resolve by merging the two tables/services into one canonical escalation path, or by cleanly separating them with distinct names if they really are different concerns (mirror the decision-making from the `care_gaps` fix: check real callers before choosing). Update both services' queries and add a regression test asserting escalations from both trigger paths appear in the same dashboard query. | Likely yes — needs the golden-rule provisioning-bundle process |
| **S266** | PostVisit Ingestion Cron Hardening | Replace F6's silent-empty/silent-null fallbacks with real error surfacing (alert on tenant-service failure, per-tenant DB-connection failure visibility, a stuck-item detector that flags sessions stuck in `'captured'` past a threshold). | No — **Done (2026-08-28)** |
| **S267** | AI Surface Contract Audit Wiring | Wire `recordExecution()` (F7) into every AI-facing service that currently only calls `buildSurfaceMetadata()` — encounter-copilot, claims-ai, radiology-ai, patient-ai, patient-intelligence. Make the `tenantDb`/`hipaaAuditService`-missing fallback path throw or alert instead of silently returning `recorded: false`. Add a CI-style audit script (matching the pattern of `audit-controller-guards.mjs`) that fails the build if a new AI-output-returning method doesn't call `recordExecution`. | No — **Done (2026-08-28)** |

### TIER 2 — High

| Sprint | Title | What It Delivers | DB provisioning touch? |
|---|---|---|---|
| **S268** | CDSS RAG & Guidelines Hardening | Make ungrounded generation visible in the response (F8) instead of silently proceeding with empty context — add a `groundingStatus` field. Decide whether the empty fallback guideline engine (F9) should be populated or removed entirely as dead code (matches the "delete vs. build" judgment call pattern from the earlier engagement). | No — **Done (2026-08-28)** |
| **S269** | Backend Silent-Catch Sweep — Workflow & Order Pipelines | Batch-fix the same-shaped silent-swallow bugs: ghost workflow creation (F11), dropped order-pipeline saves (F14), fire-and-forget follow-up generation with an unverified "nightly sweep" safety-net claim (F15 — verify the claim first), fire-and-forget explainability audit (F18). | No — **Done (2026-08-28)** |
| **S270** | AI Governance Dashboard Honesty | Replace F16's silent-empty-on-failure pattern across the 5 call sites in `ai-performance.service.ts` with real error surfacing, so a broken drift-detection query shows as broken, not as "no drift." | No — **Done (2026-08-28)** |
| **S271** | Escalation Classifier LLM Wiring | Route `classifyEscalationSignals` (F12) through the existing `PostVisitGroundedLlmService.classifyEscalationSignal()` instead of hardcoded keywords, with the keyword matcher retained only as an explicit fallback on LLM failure (not the sole mechanism). | No — **Done (2026-08-28)** |
| **S272** | Knowledge Ingestion Reliability | Fix F17 — either make ingestion synchronous with a real timeout, or add a real terminal failure state (`'failed'`) that the fire-and-forget job can transition to, so documents don't stay `'pending'` forever after a silent failure. | No |
| **S273** | Voice ID & Remaining Hygiene | Fix F13 (weak voice session IDs → `crypto.randomInt`/`randomBytes`, matching the established fix pattern). Sweep remaining Tier 3 items: F19 (HIPAA audit catches in post-visit.service.ts), F20 (surface companion-memory failures instead of silently degrading "grounded" claims), F21 (validate `recordPrediction`'s DTO), F22 (decide whether to add a REST chunk endpoint or explicitly document WebSocket-only as intentional). | No |

---

## Definition of Done (applies to every sprint above)

1. No silent `.catch(() => {})` / empty catch on any path that (a) claims success to a caller while actually failing, or (b) is on a patient-safety, compliance, or escalation-delivery path.
2. Every fix verified against the *real* calling code, not just read in isolation — confirm reachability before and after.
3. Any DB schema change goes through `getProvisioningBundles()`, never a bare `.sql` file — golden rule.
4. `tsc --noEmit` clean in every workspace touched (not just the one nominally targeted — the `version: 1` bug from the prior engagement was missed exactly because of this).
5. Add or update a `.spec.ts`/pytest test for the specific failure mode being fixed, not just the happy path.
