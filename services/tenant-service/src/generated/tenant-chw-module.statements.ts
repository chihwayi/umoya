export const TENANT_CHW_MODULE_BUNDLE_VERSION = '2026.04.09.4';

export const TENANT_CHW_MODULE_STATEMENTS = (): string[] => [
  `CREATE TABLE IF NOT EXISTS households (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    household_code VARCHAR(30) NOT NULL UNIQUE,
    head_of_household VARCHAR(200),
    address TEXT,
    village VARCHAR(100),
    ward VARCHAR(100),
    district VARCHAR(100),
    gps_lat NUMERIC(9,6),
    gps_lng NUMERIC(9,6),
    water_source VARCHAR(50),
    sanitation_type VARCHAR(50),
    assigned_chw_id UUID,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
  )`,
  `CREATE TABLE IF NOT EXISTS household_members (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    household_id UUID REFERENCES households(id) NOT NULL,
    patient_id UUID,
    member_name VARCHAR(200) NOT NULL,
    date_of_birth DATE,
    sex VARCHAR(10),
    relationship VARCHAR(50),
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
  )`,
  `CREATE TABLE IF NOT EXISTS chw_visits (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    chw_id UUID NOT NULL,
    household_id UUID REFERENCES households(id),
    patient_id UUID,
    visit_date DATE NOT NULL,
    visit_type VARCHAR(50) NOT NULL,
    muac_mm INTEGER,
    muac_classification VARCHAR(10),
    weight_kg NUMERIC(5,2),
    height_cm NUMERIC(5,2),
    temperature_celsius NUMERIC(4,1),
    referred_to_facility BOOLEAN DEFAULT false NOT NULL,
    referral_reason TEXT,
    services_provided TEXT[],
    notes TEXT,
    gps_lat NUMERIC(9,6),
    gps_lng NUMERIC(9,6),
    synced BOOLEAN DEFAULT false NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
  )`,
  `CREATE TABLE IF NOT EXISTS chw_tasks (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    assigned_to_chw_id UUID NOT NULL,
    patient_id UUID,
    household_id UUID REFERENCES households(id),
    task_type VARCHAR(50) NOT NULL,
    due_date DATE NOT NULL,
    priority VARCHAR(10) DEFAULT 'normal',
    instructions TEXT,
    status VARCHAR(20) DEFAULT 'pending' NOT NULL,
    completed_at TIMESTAMP WITH TIME ZONE,
    completion_notes TEXT,
    assigned_by UUID,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL
  )`,
  `CREATE TABLE IF NOT EXISTS chw_daily_tallies (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    chw_id UUID NOT NULL,
    tally_date DATE NOT NULL,
    households_visited INTEGER DEFAULT 0,
    anc_visits INTEGER DEFAULT 0,
    postnatal_visits INTEGER DEFAULT 0,
    sick_children_seen INTEGER DEFAULT 0,
    tb_dot_observations INTEGER DEFAULT 0,
    muac_screenings INTEGER DEFAULT 0,
    sam_cases_identified INTEGER DEFAULT 0,
    referrals_made INTEGER DEFAULT 0,
    immunizations_given INTEGER DEFAULT 0,
    dhis2_synced BOOLEAN DEFAULT false NOT NULL,
    created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW() NOT NULL,
    UNIQUE(chw_id, tally_date)
  )`,
  `CREATE INDEX IF NOT EXISTS idx_households_chw ON households(assigned_chw_id)`,
  `CREATE INDEX IF NOT EXISTS idx_chw_visits_chw ON chw_visits(chw_id, visit_date)`,
  `CREATE INDEX IF NOT EXISTS idx_chw_tasks_chw ON chw_tasks(assigned_to_chw_id, status)`,
  `CREATE INDEX IF NOT EXISTS idx_chw_tallies_chw ON chw_daily_tallies(chw_id, tally_date)`,
];
