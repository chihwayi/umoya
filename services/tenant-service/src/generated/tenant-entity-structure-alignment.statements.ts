// Generated by scripts/generate-tenant-structure-alignment.mjs
// This bundle backfills entity-declared structural objects such as indexes, unique constraints, and foreign keys.

export const TENANT_ENTITY_STRUCTURE_ALIGNMENT_BUNDLE_VERSION = '2026.06.12.1';

export const TENANT_ENTITY_STRUCTURE_ALIGNMENT_STATEMENTS = [
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'admissions'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['admission_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_admissions_admission_number
      ON admissions (admission_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'admissions'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE admissions
      ADD CONSTRAINT fk_admissions_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'admissions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['admitting_provider']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE admissions
      ADD CONSTRAINT fk_admissions_admitting_provider_users
      FOREIGN KEY (admitting_provider)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'admissions'
      AND rt.relname = 'beds'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['initial_bed_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE admissions
      ADD CONSTRAINT fk_admissions_initial_bed_id_beds
      FOREIGN KEY (initial_bed_id)
      REFERENCES beds (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'admissions'
      AND rt.relname = 'beds'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['current_bed_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE admissions
      ADD CONSTRAINT fk_admissions_current_bed_id_beds
      FOREIGN KEY (current_bed_id)
      REFERENCES beds (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'admissions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['attending_provider']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE admissions
      ADD CONSTRAINT fk_admissions_attending_provider_users
      FOREIGN KEY (attending_provider)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ai_eval_runs'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ai_surface', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ai_eval_runs_surface_created
      ON ai_eval_runs (ai_surface, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ai_eval_runs'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['run_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ai_eval_runs_status
      ON ai_eval_runs (run_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ai_ops_metrics'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['surface']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ai_ops_metrics_surface
      ON ai_ops_metrics (surface);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ai_ops_metrics'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['metric_date']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ai_ops_metrics_metric_date
      ON ai_ops_metrics (metric_date);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ai_release_gate_results'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ai_surface', 'gate_name']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ai_release_gate_results_surface_gate
      ON ai_release_gate_results (ai_surface, gate_name);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ai_release_gate_results'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['gate_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ai_release_gate_results_status
      ON ai_release_gate_results (gate_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'allergies'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE allergies
      ADD CONSTRAINT fk_allergies_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'allergies'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['recorded_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE allergies
      ADD CONSTRAINT fk_allergies_recorded_by_users
      FOREIGN KEY (recorded_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'ambient_sessions'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE ambient_sessions
      ADD CONSTRAINT fk_ambient_sessions_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'ambient_sessions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['provider_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE ambient_sessions
      ADD CONSTRAINT fk_ambient_sessions_provider_id_users
      FOREIGN KEY (provider_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_billing'
      AND rt.relname = 'surgical_cases'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['surgical_case_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE anesthesia_billing
      ADD CONSTRAINT fk_anesthesia_billing_surgical_case_id_surgical_cases
      FOREIGN KEY (surgical_case_id)
      REFERENCES surgical_cases (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_billing'
      AND rt.relname = 'anesthesia_records'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['anesthesia_record_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE anesthesia_billing
      ADD CONSTRAINT fk_anesthesia_billing_anesthesia_record_id_ane_29132611
      FOREIGN KEY (anesthesia_record_id)
      REFERENCES anesthesia_records (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_billing'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['billed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE anesthesia_billing
      ADD CONSTRAINT fk_anesthesia_billing_billed_by_users
      FOREIGN KEY (billed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_records'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['surgical_case_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_anesthesia_record_case
      ON anesthesia_records (surgical_case_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_records'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_anesthesia_record_patient
      ON anesthesia_records (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_records'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['anesthesiologist_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_anesthesia_record_provider
      ON anesthesia_records (anesthesiologist_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_records'
      AND rt.relname = 'surgical_cases'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['surgical_case_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE anesthesia_records
      ADD CONSTRAINT fk_anesthesia_records_surgical_case_id_surgical_cases
      FOREIGN KEY (surgical_case_id)
      REFERENCES surgical_cases (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_records'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE anesthesia_records
      ADD CONSTRAINT fk_anesthesia_records_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['anesthesiologist_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE anesthesia_records
      ADD CONSTRAINT fk_anesthesia_records_anesthesiologist_id_users
      FOREIGN KEY (anesthesiologist_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['crna_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE anesthesia_records
      ADD CONSTRAINT fk_anesthesia_records_crna_id_users
      FOREIGN KEY (crna_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_vitals'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['anesthesia_record_id', 'chart_time']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_anesthesia_vitals_anesthesia_record_id_chart_time
      ON anesthesia_vitals (anesthesia_record_id, chart_time);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_vitals'
      AND rt.relname = 'anesthesia_records'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['anesthesia_record_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE anesthesia_vitals
      ADD CONSTRAINT fk_anesthesia_vitals_anesthesia_record_id_anes_66d53f90
      FOREIGN KEY (anesthesia_record_id)
      REFERENCES anesthesia_records (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'anesthesia_vitals'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['recorded_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE anesthesia_vitals
      ADD CONSTRAINT fk_anesthesia_vitals_recorded_by_users
      FOREIGN KEY (recorded_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'antimicrobial_stewardship'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE antimicrobial_stewardship
      ADD CONSTRAINT fk_antimicrobial_stewardship_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'antimicrobial_stewardship'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reviewed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE antimicrobial_stewardship
      ADD CONSTRAINT fk_antimicrobial_stewardship_reviewed_by_users
      FOREIGN KEY (reviewed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'antimicrobial_stewardship'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['prescribed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE antimicrobial_stewardship
      ADD CONSTRAINT fk_antimicrobial_stewardship_prescribed_by_users
      FOREIGN KEY (prescribed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointment_waitlist'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointment_waitlist
      ADD CONSTRAINT fk_appointment_waitlist_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointment_waitlist'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['doctor_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointment_waitlist
      ADD CONSTRAINT fk_appointment_waitlist_doctor_id_users
      FOREIGN KEY (doctor_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointment_waitlist'
      AND rt.relname = 'appointments'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['scheduled_appointment_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointment_waitlist
      ADD CONSTRAINT fk_appointment_waitlist_scheduled_appointment__1088ecbd
      FOREIGN KEY (scheduled_appointment_id)
      REFERENCES appointments (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointment_waitlist'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointment_waitlist
      ADD CONSTRAINT fk_appointment_waitlist_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointments'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointments
      ADD CONSTRAINT fk_appointments_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['doctor_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointments
      ADD CONSTRAINT fk_appointments_doctor_id_users
      FOREIGN KEY (doctor_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointments
      ADD CONSTRAINT fk_appointments_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointments'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointments
      ADD CONSTRAINT fk_appointments_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['doctor_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointments
      ADD CONSTRAINT fk_appointments_doctor_id_users
      FOREIGN KEY (doctor_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'appointments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE appointments
      ADD CONSTRAINT fk_appointments_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'bank_statements'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['entry_date']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_bank_statements_date
      ON bank_statements (entry_date);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'bank_statements'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['is_matched']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_bank_statements_matched
      ON bank_statements (is_matched);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'bank_statements'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reference']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_bank_statements_reference
      ON bank_statements (reference);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'beds'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['current_patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE beds
      ADD CONSTRAINT fk_beds_current_patient_id_patients
      FOREIGN KEY (current_patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'beds'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['last_cleaned_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE beds
      ADD CONSTRAINT fk_beds_last_cleaned_by_users
      FOREIGN KEY (last_cleaned_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'billing'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE billing
      ADD CONSTRAINT fk_billing_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'billing'
      AND rt.relname = 'appointments'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['appointment_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE billing
      ADD CONSTRAINT fk_billing_appointment_id_appointments
      FOREIGN KEY (appointment_id)
      REFERENCES appointments (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'billing'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE billing
      ADD CONSTRAINT fk_billing_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'blood_donors'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE blood_donors
      ADD CONSTRAINT fk_blood_donors_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'blood_inventory'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['unit_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_blood_inventory_unit_number
      ON blood_inventory (unit_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'blood_transfusions'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE blood_transfusions
      ADD CONSTRAINT fk_blood_transfusions_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'blood_transfusions'
      AND rt.relname = 'blood_inventory'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['inventory_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE blood_transfusions
      ADD CONSTRAINT fk_blood_transfusions_inventory_id_blood_inventory
      FOREIGN KEY (inventory_id)
      REFERENCES blood_inventory (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'blood_transfusions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ordered_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE blood_transfusions
      ADD CONSTRAINT fk_blood_transfusions_ordered_by_users
      FOREIGN KEY (ordered_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'blood_transfusions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['consent_obtained_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE blood_transfusions
      ADD CONSTRAINT fk_blood_transfusions_consent_obtained_by_users
      FOREIGN KEY (consent_obtained_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'blood_transfusions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['administered_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE blood_transfusions
      ADD CONSTRAINT fk_blood_transfusions_administered_by_users
      FOREIGN KEY (administered_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'blood_transfusions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['monitored_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE blood_transfusions
      ADD CONSTRAINT fk_blood_transfusions_monitored_by_users
      FOREIGN KEY (monitored_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'care_gap_detections'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE care_gap_detections
      ADD CONSTRAINT fk_care_gap_detections_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'care_gap_detections'
      AND rt.relname = 'nurse_tasks'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['linked_task_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE care_gap_detections
      ADD CONSTRAINT fk_care_gap_detections_linked_task_id_nurse_tasks
      FOREIGN KEY (linked_task_id)
      REFERENCES nurse_tasks (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'cbhi_claims'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_cbhi_claims_claim_number
      ON cbhi_claims (claim_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'cbhi_household_members'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_cbhi_household_members_patient_id
      ON cbhi_household_members (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'cbhi_households'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['household_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_cbhi_households_household_id
      ON cbhi_households (household_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'cdss_decision_log'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE cdss_decision_log
      ADD CONSTRAINT fk_cdss_decision_log_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'cdss_decision_log'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['user_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE cdss_decision_log
      ADD CONSTRAINT fk_cdss_decision_log_user_id_users
      FOREIGN KEY (user_id)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'cdss_decision_log'
      AND rt.relname = 'clinical_outcomes'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_outcome_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE cdss_decision_log
      ADD CONSTRAINT fk_cdss_decision_log_patient_outcome_id_clinic_ad965d2d
      FOREIGN KEY (patient_outcome_id)
      REFERENCES clinical_outcomes (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'cgm_summary'
      AND rt.relname = 'diabetes_registry'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['diabetes_registry_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE cgm_summary
      ADD CONSTRAINT fk_cgm_summary_diabetes_registry_id_diabetes_registry
      FOREIGN KEY (diabetes_registry_id)
      REFERENCES diabetes_registry (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'cgm_summary'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE cgm_summary
      ADD CONSTRAINT fk_cgm_summary_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'charge_approval_notifications'
      AND rt.relname = 'admissions'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['admission_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE charge_approval_notifications
      ADD CONSTRAINT fk_charge_approval_notifications_admission_id__dcdab7f4
      FOREIGN KEY (admission_id)
      REFERENCES admissions (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'charge_approval_notifications'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE charge_approval_notifications
      ADD CONSTRAINT fk_charge_approval_notifications_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'charge_approval_notifications'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE charge_approval_notifications
      ADD CONSTRAINT fk_charge_approval_notifications_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'charge_approval_notifications'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['read_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE charge_approval_notifications
      ADD CONSTRAINT fk_charge_approval_notifications_read_by_users
      FOREIGN KEY (read_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'charge_master'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['charge_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_charge_master_charge_code
      ON charge_master (charge_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'chronic_disease_registry'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_chronic_disease_registry_patient_id
      ON chronic_disease_registry (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'claim_appeals'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_claim_appeals_claim_id
      ON claim_appeals (claim_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'claim_denial_predictions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_claim_denial_predictions_claim_id
      ON claim_denial_predictions (claim_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'claim_denial_predictions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['risk_level']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_claim_denial_predictions_risk_level
      ON claim_denial_predictions (risk_level);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'claim_risk_scores'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_claim_risk_scores_claim_id
      ON claim_risk_scores (claim_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'claim_risk_scores'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_claim_risk_scores_patient_id
      ON claim_risk_scores (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_escalation_tasks'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_clinical_escalation_tasks_patient_status
      ON clinical_escalation_tasks (patient_id, status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_escalation_tasks'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['early_warning_score_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_clinical_escalation_tasks_ews
      ON clinical_escalation_tasks (early_warning_score_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_escalation_tasks'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['due_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_clinical_escalation_tasks_due_at
      ON clinical_escalation_tasks (due_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_note_templates'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE clinical_note_templates
      ADD CONSTRAINT fk_clinical_note_templates_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_outcomes'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE clinical_outcomes
      ADD CONSTRAINT fk_clinical_outcomes_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_outcomes'
      AND rt.relname = 'appointments'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['related_appointment_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE clinical_outcomes
      ADD CONSTRAINT fk_clinical_outcomes_related_appointment_id_ap_d15b6e14
      FOREIGN KEY (related_appointment_id)
      REFERENCES appointments (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_outcomes'
      AND rt.relname = 'prescriptions'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['related_prescription_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE clinical_outcomes
      ADD CONSTRAINT fk_clinical_outcomes_related_prescription_id_p_2987afee
      FOREIGN KEY (related_prescription_id)
      REFERENCES prescriptions (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_outcomes'
      AND rt.relname = 'lab_orders'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['related_lab_order_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE clinical_outcomes
      ADD CONSTRAINT fk_clinical_outcomes_related_lab_order_id_lab_orders
      FOREIGN KEY (related_lab_order_id)
      REFERENCES lab_orders (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_outcomes'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['recorded_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE clinical_outcomes
      ADD CONSTRAINT fk_clinical_outcomes_recorded_by_users
      FOREIGN KEY (recorded_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_pathways'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['pathway_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_clinical_pathways_pathway_code
      ON clinical_pathways (pathway_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_pathways'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['last_reviewed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE clinical_pathways
      ADD CONSTRAINT fk_clinical_pathways_last_reviewed_by_users
      FOREIGN KEY (last_reviewed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'clinical_pathways'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE clinical_pathways
      ADD CONSTRAINT fk_clinical_pathways_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'consent_signatures'
      AND rt.relname = 'patient_consents'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['consent_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE consent_signatures
      ADD CONSTRAINT fk_consent_signatures_consent_id_patient_consents
      FOREIGN KEY (consent_id)
      REFERENCES patient_consents (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'consent_signatures'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['signer_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE consent_signatures
      ADD CONSTRAINT fk_consent_signatures_signer_id_users
      FOREIGN KEY (signer_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'consent_templates'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['template_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_consent_templates_template_code
      ON consent_templates (template_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'consent_templates'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE consent_templates
      ADD CONSTRAINT fk_consent_templates_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'critical_result_alerts'
      AND rt.relname = 'lab_orders'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['lab_order_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE critical_result_alerts
      ADD CONSTRAINT fk_critical_result_alerts_lab_order_id_lab_orders
      FOREIGN KEY (lab_order_id)
      REFERENCES lab_orders (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'critical_result_alerts'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE critical_result_alerts
      ADD CONSTRAINT fk_critical_result_alerts_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'critical_result_alerts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ordering_provider_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE critical_result_alerts
      ADD CONSTRAINT fk_critical_result_alerts_ordering_provider_id_users
      FOREIGN KEY (ordering_provider_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'critical_result_alerts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['acknowledged_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE critical_result_alerts
      ADD CONSTRAINT fk_critical_result_alerts_acknowledged_by_users
      FOREIGN KEY (acknowledged_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'cross_border_patient_flags'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_cross_border_patient_flags_patient_id
      ON cross_border_patient_flags (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'developmental_milestones'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE developmental_milestones
      ADD CONSTRAINT fk_developmental_milestones_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'developmental_milestones'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['assessed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE developmental_milestones
      ADD CONSTRAINT fk_developmental_milestones_assessed_by_users
      FOREIGN KEY (assessed_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_alerts'
      AND rt.relname = 'diabetes_registry'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['diabetes_registry_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_alerts
      ADD CONSTRAINT fk_diabetes_alerts_diabetes_registry_id_diabet_4e6fd3ee
      FOREIGN KEY (diabetes_registry_id)
      REFERENCES diabetes_registry (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_alerts'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_alerts
      ADD CONSTRAINT fk_diabetes_alerts_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_care_bundle'
      AND rt.relname = 'diabetes_registry'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['diabetes_registry_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_care_bundle
      ADD CONSTRAINT fk_diabetes_care_bundle_diabetes_registry_id_d_25d38a66
      FOREIGN KEY (diabetes_registry_id)
      REFERENCES diabetes_registry (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_care_bundle'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_care_bundle
      ADD CONSTRAINT fk_diabetes_care_bundle_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_complication_screening'
      AND rt.relname = 'diabetes_registry'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['diabetes_registry_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_complication_screening
      ADD CONSTRAINT fk_diabetes_complication_screening_diabetes_re_48939946
      FOREIGN KEY (diabetes_registry_id)
      REFERENCES diabetes_registry (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_complication_screening'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_complication_screening
      ADD CONSTRAINT fk_diabetes_complication_screening_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_device_integration'
      AND rt.relname = 'diabetes_registry'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['diabetes_registry_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_device_integration
      ADD CONSTRAINT fk_diabetes_device_integration_diabetes_regist_ec51bf2a
      FOREIGN KEY (diabetes_registry_id)
      REFERENCES diabetes_registry (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_device_integration'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_device_integration
      ADD CONSTRAINT fk_diabetes_device_integration_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_education_sessions'
      AND rt.relname = 'diabetes_registry'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['diabetes_registry_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_education_sessions
      ADD CONSTRAINT fk_diabetes_education_sessions_diabetes_regist_6c25f7cd
      FOREIGN KEY (diabetes_registry_id)
      REFERENCES diabetes_registry (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_education_sessions'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_education_sessions
      ADD CONSTRAINT fk_diabetes_education_sessions_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_medications'
      AND rt.relname = 'diabetes_registry'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['diabetes_registry_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_medications
      ADD CONSTRAINT fk_diabetes_medications_diabetes_registry_id_d_19101f62
      FOREIGN KEY (diabetes_registry_id)
      REFERENCES diabetes_registry (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_medications'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_medications
      ADD CONSTRAINT fk_diabetes_medications_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_registry'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_diabetes_registry_patient_id
      ON diabetes_registry (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'diabetes_registry'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE diabetes_registry
      ADD CONSTRAINT fk_diabetes_registry_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'dicom_series'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['imaging_order_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_dicom_series_imaging_order_id
      ON dicom_series (imaging_order_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'dicom_studies'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['study_uid']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_dicom_studies_study_uid
      ON dicom_studies (study_uid);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'discharges'
      AND rt.relname = 'admissions'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['admission_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE discharges
      ADD CONSTRAINT fk_discharges_admission_id_admissions
      FOREIGN KEY (admission_id)
      REFERENCES admissions (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'discharges'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE discharges
      ADD CONSTRAINT fk_discharges_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'discharges'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['discharge_provider']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE discharges
      ADD CONSTRAINT fk_discharges_discharge_provider_users
      FOREIGN KEY (discharge_provider)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'doctor_availability'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['doctor_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE doctor_availability
      ADD CONSTRAINT fk_doctor_availability_doctor_id_users
      FOREIGN KEY (doctor_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'doctor_availability'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE doctor_availability
      ADD CONSTRAINT fk_doctor_availability_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'doctor_availability'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE doctor_availability
      ADD CONSTRAINT fk_doctor_availability_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'drug_interactions'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['drug1_id', 'drug2_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_drug_interactions_drug1_id_drug2_id
      ON drug_interactions (drug1_id, drug2_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'drug_interactions'
      AND rt.relname = 'drugs'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['drug1_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE drug_interactions
      ADD CONSTRAINT fk_drug_interactions_drug1_id_drugs
      FOREIGN KEY (drug1_id)
      REFERENCES drugs (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'drug_interactions'
      AND rt.relname = 'drugs'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['drug2_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE drug_interactions
      ADD CONSTRAINT fk_drug_interactions_drug2_id_drugs
      FOREIGN KEY (drug2_id)
      REFERENCES drugs (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ed_visits'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ed_visit_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_ed_visits_ed_visit_number
      ON ed_visits (ed_visit_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'ed_visits'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE ed_visits
      ADD CONSTRAINT fk_ed_visits_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'ed_visits'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['triage_completed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE ed_visits
      ADD CONSTRAINT fk_ed_visits_triage_completed_by_users
      FOREIGN KEY (triage_completed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'ed_visits'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['attending_provider']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE ed_visits
      ADD CONSTRAINT fk_ed_visits_attending_provider_users
      FOREIGN KEY (attending_provider)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'ed_visits'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['primary_nurse']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE ed_visits
      ADD CONSTRAINT fk_ed_visits_primary_nurse_users
      FOREIGN KEY (primary_nurse)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'encounter_copilot_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_encounter_copilot_sessions_patient_created
      ON encounter_copilot_sessions (patient_id, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'encounter_copilot_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['appointment_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_encounter_copilot_sessions_appointment
      ON encounter_copilot_sessions (appointment_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'encounter_copilot_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_encounter_copilot_sessions_status
      ON encounter_copilot_sessions (status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'encounter_precharts'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['appointment_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_encounter_precharts_appointment_id
      ON encounter_precharts (appointment_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'encounter_precharts'
      AND rt.relname = 'appointments'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['appointment_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE encounter_precharts
      ADD CONSTRAINT fk_encounter_precharts_appointment_id_appointments
      FOREIGN KEY (appointment_id)
      REFERENCES appointments (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'encounter_precharts'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE encounter_precharts
      ADD CONSTRAINT fk_encounter_precharts_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'exchange_rates'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['base_currency', 'quote_currency']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_exchange_rates_base_currency_quote_currency
      ON exchange_rates (base_currency, quote_currency);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'fee_schedule_items'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['fee_schedule_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_fee_schedule_items_fee_schedule_id
      ON fee_schedule_items (fee_schedule_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'fee_schedule_items'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['cpt_code']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_fee_schedule_items_cpt_code
      ON fee_schedule_items (cpt_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'financial_clearance_assessments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_financial_clearance_assessments_patient_id
      ON financial_clearance_assessments (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'financial_clearance_assessments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_financial_clearance_assessments_claim_id
      ON financial_clearance_assessments (claim_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'financial_clearance_assessments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['eligibility_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_financial_clearance_assessments_eligibility_status
      ON financial_clearance_assessments (eligibility_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'financial_hardship_referrals'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_financial_hardship_referrals_patient_id
      ON financial_hardship_referrals (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'financial_quote_assessments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['transaction_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_financial_quote_assessments_transaction_id
      ON financial_quote_assessments (transaction_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'financial_quote_assessments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_financial_quote_assessments_patient_id
      ON financial_quote_assessments (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'financial_quote_assessments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['quote_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_financial_quote_assessments_quote_status
      ON financial_quote_assessments (quote_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'financial_quote_assessments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['quoted_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_financial_quote_assessments_quoted_at
      ON financial_quote_assessments (quoted_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'glucose_monitoring'
      AND rt.relname = 'diabetes_registry'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['diabetes_registry_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE glucose_monitoring
      ADD CONSTRAINT fk_glucose_monitoring_diabetes_registry_id_dia_efa69f29
      FOREIGN KEY (diabetes_registry_id)
      REFERENCES diabetes_registry (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'glucose_monitoring'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE glucose_monitoring
      ADD CONSTRAINT fk_glucose_monitoring_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'growth_measurements'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE growth_measurements
      ADD CONSTRAINT fk_growth_measurements_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'growth_measurements'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['measured_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE growth_measurements
      ADD CONSTRAINT fk_growth_measurements_measured_by_users
      FOREIGN KEY (measured_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'health_education_content'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['category']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_health_education_content_category
      ON health_education_content (category);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'health_education_content'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['language']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_health_education_content_language
      ON health_education_content (language);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'home_birth_records'
      AND rt.relname = 'tba_register'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tba_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE home_birth_records
      ADD CONSTRAINT fk_home_birth_records_tba_id_tba_register
      FOREIGN KEY (tba_id)
      REFERENCES tba_register (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'households'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['household_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_households_household_code
      ON households (household_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'idempotency_keys'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_idempotency_keys_created_at
      ON idempotency_keys (created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'idempotency_keys'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['op_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_idempotency_keys_op_id
      ON idempotency_keys (op_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'imaging_order_ai_reviews'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['imaging_order_id', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_imaging_order_ai_reviews_order_created
      ON imaging_order_ai_reviews (imaging_order_id, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'imaging_order_ai_reviews'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'appropriateness_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_imaging_order_ai_reviews_patient_status
      ON imaging_order_ai_reviews (patient_id, appropriateness_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'immunizations'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['immunization_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_immunizations_immunization_number
      ON immunizations (immunization_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'immunizations'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE immunizations
      ADD CONSTRAINT fk_immunizations_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'immunizations'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['administered_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE immunizations
      ADD CONSTRAINT fk_immunizations_administered_by_users
      FOREIGN KEY (administered_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'immunizations'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ordering_provider']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE immunizations
      ADD CONSTRAINT fk_immunizations_ordering_provider_users
      FOREIGN KEY (ordering_provider)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'immunizations'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE immunizations
      ADD CONSTRAINT fk_immunizations_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'inbox_items'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['user_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE inbox_items
      ADD CONSTRAINT fk_inbox_items_user_id_users
      FOREIGN KEY (user_id)
      REFERENCES users (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'inbox_items'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE inbox_items
      ADD CONSTRAINT fk_inbox_items_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'incidental_finding_followups'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['imaging_report_id', 'status', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_incidental_followups_report_status
      ON incidental_finding_followups (imaging_report_id, status, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'incidental_finding_followups'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'status', 'due_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_incidental_followups_patient_due
      ON incidental_finding_followups (patient_id, status, due_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'infection_surveillance'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE infection_surveillance
      ADD CONSTRAINT fk_infection_surveillance_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'infection_surveillance'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['detected_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE infection_surveillance
      ADD CONSTRAINT fk_infection_surveillance_detected_by_users
      FOREIGN KEY (detected_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'insulin_regimens'
      AND rt.relname = 'diabetes_registry'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['diabetes_registry_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE insulin_regimens
      ADD CONSTRAINT fk_insulin_regimens_diabetes_registry_id_diabe_584d0145
      FOREIGN KEY (diabetes_registry_id)
      REFERENCES diabetes_registry (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'insulin_regimens'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE insulin_regimens
      ADD CONSTRAINT fk_insulin_regimens_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'insurance_ocr_results'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_insurance_ocr_results_patient_id
      ON insurance_ocr_results (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'insurance_verifications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_insurance_verifications_patient_id
      ON insurance_verifications (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'insurance_verifications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['appointment_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_insurance_verifications_appointment_id
      ON insurance_verifications (appointment_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'isolation_precautions'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE isolation_precautions
      ADD CONSTRAINT fk_isolation_precautions_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'isolation_precautions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ordered_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE isolation_precautions
      ADD CONSTRAINT fk_isolation_precautions_ordered_by_users
      FOREIGN KEY (ordered_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'isolation_precautions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['discontinued_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE isolation_precautions
      ADD CONSTRAINT fk_isolation_precautions_discontinued_by_users
      FOREIGN KEY (discontinued_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'lab_order_sets'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['set_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_lab_order_sets_set_code
      ON lab_order_sets (set_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'lab_orders'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE lab_orders
      ADD CONSTRAINT fk_lab_orders_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'lab_orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ordering_provider_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE lab_orders
      ADD CONSTRAINT fk_lab_orders_ordering_provider_id_users
      FOREIGN KEY (ordering_provider_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'lab_orders'
      AND rt.relname = 'medical_records'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['medical_record_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE lab_orders
      ADD CONSTRAINT fk_lab_orders_medical_record_id_medical_records
      FOREIGN KEY (medical_record_id)
      REFERENCES medical_records (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'lab_orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['collected_by_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE lab_orders
      ADD CONSTRAINT fk_lab_orders_collected_by_id_users
      FOREIGN KEY (collected_by_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'lab_orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reviewed_by_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE lab_orders
      ADD CONSTRAINT fk_lab_orders_reviewed_by_id_users
      FOREIGN KEY (reviewed_by_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'lab_tests'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['loinc_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_lab_tests_loinc_code
      ON lab_tests (loinc_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_claim_lines'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_aid_claim_lines_claim_id
      ON medical_aid_claim_lines (claim_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_claim_lines'
      AND rt.relname = 'medical_aid_claims'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medical_aid_claim_lines
      ADD CONSTRAINT fk_medical_aid_claim_lines_claim_id_medical_aid_claims
      FOREIGN KEY (claim_id)
      REFERENCES medical_aid_claims (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_claim_submissions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['transaction_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_aid_claim_submissions_transaction_id
      ON medical_aid_claim_submissions (transaction_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_claim_submissions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['provider_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_aid_claim_submissions_provider_id
      ON medical_aid_claim_submissions (provider_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_claims'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_medical_aid_claims_claim_number
      ON medical_aid_claims (claim_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_claims'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medical_aid_claims
      ADD CONSTRAINT fk_medical_aid_claims_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_claims'
      AND rt.relname = 'billing'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['billing_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medical_aid_claims
      ADD CONSTRAINT fk_medical_aid_claims_billing_id_billing
      FOREIGN KEY (billing_id)
      REFERENCES billing (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_eligibility_checks'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_aid_eligibility_checks_patient_id
      ON medical_aid_eligibility_checks (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_eligibility_checks'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['provider_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_aid_eligibility_checks_provider_id
      ON medical_aid_eligibility_checks (provider_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_providers'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['name']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_medical_aid_providers_name
      ON medical_aid_providers (name);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_providers'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_medical_aid_providers_code
      ON medical_aid_providers (code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_remittance_lines'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['remittance_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_aid_remittance_lines_remittance_id
      ON medical_aid_remittance_lines (remittance_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_remittance_lines'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_number']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_aid_remittance_lines_claim_number
      ON medical_aid_remittance_lines (claim_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_remittance_lines'
      AND rt.relname = 'medical_aid_remittances'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['remittance_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medical_aid_remittance_lines
      ADD CONSTRAINT fk_medical_aid_remittance_lines_remittance_id__8d205be4
      FOREIGN KEY (remittance_id)
      REFERENCES medical_aid_remittances (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_remittances'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['provider_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_aid_remittances_provider_id
      ON medical_aid_remittances (provider_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_tariff_codes'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['schedule']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_aid_tariff_codes_schedule
      ON medical_aid_tariff_codes (schedule);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_aid_tariff_codes'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_medical_aid_tariff_codes_code
      ON medical_aid_tariff_codes (code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_records'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_records_patient_id
      ON medical_records (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_records'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['record_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medical_records_type
      ON medical_records (record_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_records'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medical_records
      ADD CONSTRAINT fk_medical_records_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_records'
      AND rt.relname = 'appointments'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['appointment_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medical_records
      ADD CONSTRAINT fk_medical_records_appointment_id_appointments
      FOREIGN KEY (appointment_id)
      REFERENCES appointments (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['doctor_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medical_records
      ADD CONSTRAINT fk_medical_records_doctor_id_users
      FOREIGN KEY (doctor_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medical_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medical_records
      ADD CONSTRAINT fk_medical_records_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_adherence'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['medication_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medication_adherence_medication_id
      ON medication_adherence (medication_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_adherence'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medication_adherence_patient_id
      ON medication_adherence (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_adherence'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['adherence_date']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medication_adherence_adherence_date
      ON medication_adherence (adherence_date);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_adherence'
      AND c.contype = 'u'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['medication_id', 'adherence_date']::text[]
  ) AND NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_adherence'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['medication_id', 'adherence_date']::text[]
  ) THEN
    ALTER TABLE medication_adherence
      ADD CONSTRAINT medication_adherence_medication_id_adherence_date_key
      UNIQUE (medication_id, adherence_date);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_administration_records'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medication_administration_records
      ADD CONSTRAINT fk_medication_administration_records_patient_i_5d77d0c2
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_administration_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['administered_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medication_administration_records
      ADD CONSTRAINT fk_medication_administration_records_administe_746a37a2
      FOREIGN KEY (administered_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_administration_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['witnessed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medication_administration_records
      ADD CONSTRAINT fk_medication_administration_records_witnessed_by_users
      FOREIGN KEY (witnessed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_alerts'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medication_alerts
      ADD CONSTRAINT fk_medication_alerts_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_alerts'
      AND rt.relname = 'medication_administration_records'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['mar_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medication_alerts
      ADD CONSTRAINT fk_medication_alerts_mar_id_medication_adminis_4f349837
      FOREIGN KEY (mar_id)
      REFERENCES medication_administration_records (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_alerts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['acknowledged_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE medication_alerts
      ADD CONSTRAINT fk_medication_alerts_acknowledged_by_users
      FOREIGN KEY (acknowledged_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_reconciliation_ai_reviews'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_med_recon_ai_reviews_patient_created
      ON medication_reconciliation_ai_reviews (patient_id, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_reconciliation_ai_reviews'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['review_status', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_med_recon_ai_reviews_status
      ON medication_reconciliation_ai_reviews (review_status, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_reconciliation_log'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medication_reconciliation_log_patient_id
      ON medication_reconciliation_log (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_reconciliation_log'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reconciliation_date']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medication_reconciliation_log_reconciliation_date
      ON medication_reconciliation_log (reconciliation_date);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'medication_reconciliation_log'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reconciliation_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_medication_reconciliation_log_reconciliation_type
      ON medication_reconciliation_log (reconciliation_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'mfl_facilities'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['mfl_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_mfl_facilities_mfl_code
      ON mfl_facilities (mfl_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'model_cards'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['model_name']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_model_cards_model_name
      ON model_cards (model_name);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'mpox_lesion_assessments'
      AND rt.relname = 'vhf_cases'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['vhf_case_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE mpox_lesion_assessments
      ADD CONSTRAINT fk_mpox_lesion_assessments_vhf_case_id_vhf_cases
      FOREIGN KEY (vhf_case_id)
      REFERENCES vhf_cases (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ncid_duplicate_flags'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id_a']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ncid_duplicate_flags_patient_id_a
      ON ncid_duplicate_flags (patient_id_a);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ncid_duplicate_flags'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id_b']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ncid_duplicate_flags_patient_id_b
      ON ncid_duplicate_flags (patient_id_b);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ncid_duplicate_flags'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['resolution_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ncid_duplicate_flags_resolution_status
      ON ncid_duplicate_flags (resolution_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ncid_programme_linkages'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ncid_programme_linkages_patient_id
      ON ncid_programme_linkages (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ncid_programme_linkages'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['programme']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ncid_programme_linkages_programme
      ON ncid_programme_linkages (programme);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ncid_registrations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id_number_hash']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ncid_registrations_id_number_hash
      ON ncid_registrations (id_number_hash);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ncid_registrations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_ncid_registrations_patient_id
      ON ncid_registrations (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'neonatal_records'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE neonatal_records
      ADD CONSTRAINT fk_neonatal_records_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'neonatal_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['attending_clinician_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE neonatal_records
      ADD CONSTRAINT fk_neonatal_records_attending_clinician_id_users
      FOREIGN KEY (attending_clinician_id)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'notification_campaign_recipients'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['campaign_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_notification_campaign_recipients_campaign_id
      ON notification_campaign_recipients (campaign_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'notification_campaign_recipients'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_notification_campaign_recipients_patient_id
      ON notification_campaign_recipients (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'notification_campaigns'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_notification_campaigns_status
      ON notification_campaigns (status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'notification_log'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_notification_log_created_at
      ON notification_log (created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'notification_templates'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['template_key']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_notification_templates_template_key
      ON notification_templates (template_key);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'notification_trigger_configs'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['trigger_key']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_notification_trigger_configs_trigger_key
      ON notification_trigger_configs (trigger_key);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_copilot_alert_events'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['user_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_copilot_alert_events
      ADD CONSTRAINT fk_nurse_copilot_alert_events_user_id_users
      FOREIGN KEY (user_id)
      REFERENCES users (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_copilot_alert_events'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_copilot_alert_events
      ADD CONSTRAINT fk_nurse_copilot_alert_events_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_copilot_task_events'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['user_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_copilot_task_events
      ADD CONSTRAINT fk_nurse_copilot_task_events_user_id_users
      FOREIGN KEY (user_id)
      REFERENCES users (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_copilot_task_events'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_copilot_task_events
      ADD CONSTRAINT fk_nurse_copilot_task_events_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_cross_module_workflow_state'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_cross_module_workflow_state
      ADD CONSTRAINT fk_nurse_cross_module_workflow_state_patient_i_0983fe54
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_cross_module_workflow_state'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['destination_user_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_cross_module_workflow_state
      ADD CONSTRAINT fk_nurse_cross_module_workflow_state_destinati_e0d20f90
      FOREIGN KEY (destination_user_id)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_cross_module_workflow_state'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['acknowledged_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_cross_module_workflow_state
      ADD CONSTRAINT fk_nurse_cross_module_workflow_state_acknowled_0bce2b59
      FOREIGN KEY (acknowledged_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_cross_module_workflow_state'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['completed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_cross_module_workflow_state
      ADD CONSTRAINT fk_nurse_cross_module_workflow_state_completed_by_users
      FOREIGN KEY (completed_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_handoff_workflow_state'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_handoff_workflow_state
      ADD CONSTRAINT fk_nurse_handoff_workflow_state_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_handoff_workflow_state'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['finalized_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_handoff_workflow_state
      ADD CONSTRAINT fk_nurse_handoff_workflow_state_finalized_by_users
      FOREIGN KEY (finalized_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_handoff_workflow_state'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reviewed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_handoff_workflow_state
      ADD CONSTRAINT fk_nurse_handoff_workflow_state_reviewed_by_users
      FOREIGN KEY (reviewed_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_handoff_workflow_state'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['shared_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_handoff_workflow_state
      ADD CONSTRAINT fk_nurse_handoff_workflow_state_shared_by_users
      FOREIGN KEY (shared_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_tasks'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_tasks
      ADD CONSTRAINT fk_nurse_tasks_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_tasks'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['assigned_to']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_tasks
      ADD CONSTRAINT fk_nurse_tasks_assigned_to_users
      FOREIGN KEY (assigned_to)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'nurse_tasks'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['completed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE nurse_tasks
      ADD CONSTRAINT fk_nurse_tasks_completed_by_users
      FOREIGN KEY (completed_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'openmrs_patient_links'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['openmrs_uuid']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_openmrs_patient_links_openmrs_uuid
      ON openmrs_patient_links (openmrs_uuid);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'operating_rooms'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['room_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_operating_rooms_room_number
      ON operating_rooms (room_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'order_appropriateness_reviews'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['encounter_copilot_session_id', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_order_appropriateness_reviews_session
      ON order_appropriateness_reviews (encounter_copilot_session_id, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'order_appropriateness_reviews'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'appropriateness_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_order_appropriateness_reviews_patient_status
      ON order_appropriateness_reviews (patient_id, appropriateness_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'orders'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE orders
      ADD CONSTRAINT fk_orders_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'orders'
      AND rt.relname = 'appointments'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['appointment_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE orders
      ADD CONSTRAINT fk_orders_appointment_id_appointments
      FOREIGN KEY (appointment_id)
      REFERENCES appointments (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['doctor_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE orders
      ADD CONSTRAINT fk_orders_doctor_id_users
      FOREIGN KEY (doctor_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['authorized_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE orders
      ADD CONSTRAINT fk_orders_authorized_by_users
      FOREIGN KEY (authorized_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['executed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE orders
      ADD CONSTRAINT fk_orders_executed_by_users
      FOREIGN KEY (executed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'outcome_learning_jobs'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['feedback_log_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_outcome_learning_jobs_feedback_log_id
      ON outcome_learning_jobs (feedback_log_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pacu_records'
      AND rt.relname = 'surgical_cases'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['surgical_case_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pacu_records
      ADD CONSTRAINT fk_pacu_records_surgical_case_id_surgical_cases
      FOREIGN KEY (surgical_case_id)
      REFERENCES surgical_cases (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pacu_records'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pacu_records
      ADD CONSTRAINT fk_pacu_records_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pacu_records'
      AND rt.relname = 'anesthesia_records'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['anesthesia_record_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pacu_records
      ADD CONSTRAINT fk_pacu_records_anesthesia_record_id_anesthesia_records
      FOREIGN KEY (anesthesia_record_id)
      REFERENCES anesthesia_records (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pacu_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['pacu_nurse_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pacu_records
      ADD CONSTRAINT fk_pacu_records_pacu_nurse_id_users
      FOREIGN KEY (pacu_nurse_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pacu_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['discharge_approved_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pacu_records
      ADD CONSTRAINT fk_pacu_records_discharge_approved_by_users
      FOREIGN KEY (discharge_approved_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_ai_escalations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'status', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_ai_escalations_patient_status
      ON patient_ai_escalations (patient_id, status, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_ai_escalations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_ai_session_id', 'status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_ai_escalations_session_status
      ON patient_ai_escalations (patient_ai_session_id, status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_ai_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_ai_sessions_patient_created
      ON patient_ai_sessions (patient_id, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_ai_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_type', 'status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_ai_sessions_type_status
      ON patient_ai_sessions (session_type, status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_ai_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['source_session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_ai_sessions_source_session
      ON patient_ai_sessions (source_session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_ai_snapshots'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_ai_snapshots_patient_id
      ON patient_ai_snapshots (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_charges'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_charges
      ADD CONSTRAINT fk_patient_charges_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_charges'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ordering_provider']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_charges
      ADD CONSTRAINT fk_patient_charges_ordering_provider_users
      FOREIGN KEY (ordering_provider)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_charges'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['captured_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_charges
      ADD CONSTRAINT fk_patient_charges_captured_by_users
      FOREIGN KEY (captured_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_charges'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reviewed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_charges
      ADD CONSTRAINT fk_patient_charges_reviewed_by_users
      FOREIGN KEY (reviewed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_charges'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['approved_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_charges
      ADD CONSTRAINT fk_patient_charges_approved_by_users
      FOREIGN KEY (approved_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_consents'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['consent_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_patient_consents_consent_number
      ON patient_consents (consent_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_consents'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_consents
      ADD CONSTRAINT fk_patient_consents_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_consents'
      AND rt.relname = 'consent_templates'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['template_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_consents
      ADD CONSTRAINT fk_patient_consents_template_id_consent_templates
      FOREIGN KEY (template_id)
      REFERENCES consent_templates (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_consents'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['presented_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_consents
      ADD CONSTRAINT fk_patient_consents_presented_by_users
      FOREIGN KEY (presented_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_consents'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['revoked_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_consents
      ADD CONSTRAINT fk_patient_consents_revoked_by_users
      FOREIGN KEY (revoked_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_consents'
      AND rt.relname = 'patient_consents'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['superseded_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_consents
      ADD CONSTRAINT fk_patient_consents_superseded_by_patient_consents
      FOREIGN KEY (superseded_by)
      REFERENCES patient_consents (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_early_warning_scores'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_early_warning_scores_patient_id
      ON patient_early_warning_scores (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_early_warning_scores'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['risk_level']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_early_warning_scores_risk_level
      ON patient_early_warning_scores (risk_level);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_family_access'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_family_access_patient_id
      ON patient_family_access (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_followup_orchestrations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'status', 'due_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_followup_orchestrations_patient_status
      ON patient_followup_orchestrations (patient_id, status, due_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_followup_orchestrations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_ai_session_id', 'status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_followup_orchestrations_session_status
      ON patient_followup_orchestrations (patient_ai_session_id, status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_medications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_medications_patient_id
      ON patient_medications (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_medications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['snomed_concept_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_medications_snomed_concept_id
      ON patient_medications (snomed_concept_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_medications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['medication_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_medications_medication_type
      ON patient_medications (medication_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_medications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['start_date']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_medications_start_date
      ON patient_medications (start_date);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_medications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['end_date']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_medications_end_date
      ON patient_medications (end_date);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_medications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['prescription_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_medications_prescription_id
      ON patient_medications (prescription_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_medications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_medications_status
      ON patient_medications (status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_medications'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reconciliation_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_medications_reconciliation_status
      ON patient_medications (reconciliation_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_portal_payments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_portal_payments_patient_id
      ON patient_portal_payments (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_portal_payments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['bill_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_portal_payments_bill_id
      ON patient_portal_payments (bill_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_risk_scores'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'scored_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_risk_scores_patient_id_scored_at
      ON patient_risk_scores (patient_id, scored_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_risk_scores'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tenant_id', 'score_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_risk_scores_tenant_id_score_type
      ON patient_risk_scores (tenant_id, score_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_risk_tiers'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_risk_tiers_patient_id
      ON patient_risk_tiers (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_risk_tiers'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tier']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_risk_tiers_tier
      ON patient_risk_tiers (tier);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_sdoh'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_sdoh
      ADD CONSTRAINT fk_patient_sdoh_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_transfers'
      AND rt.relname = 'admissions'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['admission_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_transfers
      ADD CONSTRAINT fk_patient_transfers_admission_id_admissions
      FOREIGN KEY (admission_id)
      REFERENCES admissions (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_transfers'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_transfers
      ADD CONSTRAINT fk_patient_transfers_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_transfers'
      AND rt.relname = 'beds'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['from_bed_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_transfers
      ADD CONSTRAINT fk_patient_transfers_from_bed_id_beds
      FOREIGN KEY (from_bed_id)
      REFERENCES beds (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_transfers'
      AND rt.relname = 'beds'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['to_bed_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_transfers
      ADD CONSTRAINT fk_patient_transfers_to_bed_id_beds
      FOREIGN KEY (to_bed_id)
      REFERENCES beds (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_transfers'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['accepting_provider']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_transfers
      ADD CONSTRAINT fk_patient_transfers_accepting_provider_users
      FOREIGN KEY (accepting_provider)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_transfers'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['transferring_provider']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE patient_transfers
      ADD CONSTRAINT fk_patient_transfers_transferring_provider_users
      FOREIGN KEY (transferring_provider)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_vital_baselines'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'metric_name']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS idx_patient_vital_baselines_patient_metric
      ON patient_vital_baselines (patient_id, metric_name);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patient_vital_baselines'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_patient_vital_baselines_updated_at
      ON patient_vital_baselines (updated_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patients'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_patients_patient_number
      ON patients (patient_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'patients'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_patients_id_number
      ON patients (id_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_anomaly_flags'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_anomaly_flags_status
      ON payment_anomaly_flags (status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_anomaly_flags'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['severity']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_anomaly_flags_severity
      ON payment_anomaly_flags (severity);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_anomaly_flags'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['bank_entry_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_anomaly_flags_bank_entry_id
      ON payment_anomaly_flags (bank_entry_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_anomaly_flags'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['payment_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_anomaly_flags_payment_id
      ON payment_anomaly_flags (payment_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_anomaly_flags'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['detected_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_anomaly_flags_detected_at
      ON payment_anomaly_flags (detected_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_anomaly_flags'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['fingerprint']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS idx_payment_anomaly_flags_fingerprint
      ON payment_anomaly_flags (fingerprint);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_gateway_configurations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['provider_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_gateway_configurations_provider_type
      ON payment_gateway_configurations (provider_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_gateway_configurations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['is_active']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_gateway_configurations_is_active
      ON payment_gateway_configurations (is_active);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_provider_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['transaction_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_provider_events_transaction_id
      ON payment_provider_events (transaction_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_provider_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['provider_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_provider_events_provider_type
      ON payment_provider_events (provider_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_provider_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['event_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_provider_events_event_type
      ON payment_provider_events (event_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_reconciliations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['bank_entry_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_reconciliations_bank_entry_id
      ON payment_reconciliations (bank_entry_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_reconciliations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['payment_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_reconciliations_payment_id
      ON payment_reconciliations (payment_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_verification_attempts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['transaction_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_verification_attempts_transaction_id
      ON payment_verification_attempts (transaction_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'payment_verification_attempts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['outcome']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_payment_verification_attempts_outcome
      ON payment_verification_attempts (outcome);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'pdmp_checks'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_pdmp_checks_patient_id
      ON pdmp_checks (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'pediatric_profiles'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_pediatric_profiles_patient_id
      ON pediatric_profiles (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pediatric_profiles'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pediatric_profiles
      ADD CONSTRAINT fk_pediatric_profiles_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_alerts'
      AND rt.relname = 'pharmacy_inventory'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['inventory_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_alerts
      ADD CONSTRAINT fk_pharmacy_alerts_inventory_id_pharmacy_inventory
      FOREIGN KEY (inventory_id)
      REFERENCES pharmacy_inventory (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_alerts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['acknowledged_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_alerts
      ADD CONSTRAINT fk_pharmacy_alerts_acknowledged_by_users
      FOREIGN KEY (acknowledged_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_alerts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_alerts
      ADD CONSTRAINT fk_pharmacy_alerts_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensing_anomalies'
      AND rt.relname = 'pharmacy_dispensings'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['dispensing_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensing_anomalies
      ADD CONSTRAINT fk_pharmacy_dispensing_anomalies_dispensing_id_0ba9fdde
      FOREIGN KEY (dispensing_id)
      REFERENCES pharmacy_dispensings (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensing_anomalies'
      AND rt.relname = 'pharmacy_dispensing_items'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['dispensing_item_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensing_anomalies
      ADD CONSTRAINT fk_pharmacy_dispensing_anomalies_dispensing_it_72557f72
      FOREIGN KEY (dispensing_item_id)
      REFERENCES pharmacy_dispensing_items (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensing_anomalies'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensing_anomalies
      ADD CONSTRAINT fk_pharmacy_dispensing_anomalies_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensing_anomalies'
      AND rt.relname = 'pharmacy_inventory'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['inventory_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensing_anomalies
      ADD CONSTRAINT fk_pharmacy_dispensing_anomalies_inventory_id__1d59192d
      FOREIGN KEY (inventory_id)
      REFERENCES pharmacy_inventory (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensing_anomalies'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reviewed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensing_anomalies
      ADD CONSTRAINT fk_pharmacy_dispensing_anomalies_reviewed_by_users
      FOREIGN KEY (reviewed_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensing_items'
      AND rt.relname = 'pharmacy_dispensings'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['dispensing_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensing_items
      ADD CONSTRAINT fk_pharmacy_dispensing_items_dispensing_id_pha_780cab0c
      FOREIGN KEY (dispensing_id)
      REFERENCES pharmacy_dispensings (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensing_items'
      AND rt.relname = 'pharmacy_inventory'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['inventory_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensing_items
      ADD CONSTRAINT fk_pharmacy_dispensing_items_inventory_id_phar_dc499e91
      FOREIGN KEY (inventory_id)
      REFERENCES pharmacy_inventory (id) ON DELETE RESTRICT;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensings'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['dispensing_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_pharmacy_dispensings_dispensing_number
      ON pharmacy_dispensings (dispensing_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensings'
      AND rt.relname = 'prescriptions'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['prescription_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensings
      ADD CONSTRAINT fk_pharmacy_dispensings_prescription_id_prescriptions
      FOREIGN KEY (prescription_id)
      REFERENCES prescriptions (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensings'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensings
      ADD CONSTRAINT fk_pharmacy_dispensings_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensings'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['dispensed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensings
      ADD CONSTRAINT fk_pharmacy_dispensings_dispensed_by_users
      FOREIGN KEY (dispensed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensings'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensings
      ADD CONSTRAINT fk_pharmacy_dispensings_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_dispensings'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_dispensings
      ADD CONSTRAINT fk_pharmacy_dispensings_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_formulary'
      AND rt.relname = 'drugs'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['drug_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_formulary
      ADD CONSTRAINT fk_pharmacy_formulary_drug_id_drugs
      FOREIGN KEY (drug_id)
      REFERENCES drugs (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_formulary'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_formulary
      ADD CONSTRAINT fk_pharmacy_formulary_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_formulary'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_formulary
      ADD CONSTRAINT fk_pharmacy_formulary_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_inventory'
      AND rt.relname = 'drugs'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['drug_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_inventory
      ADD CONSTRAINT fk_pharmacy_inventory_drug_id_drugs
      FOREIGN KEY (drug_id)
      REFERENCES drugs (id) ON DELETE RESTRICT;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_inventory'
      AND rt.relname = 'pharmacy_suppliers'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['supplier_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_inventory
      ADD CONSTRAINT fk_pharmacy_inventory_supplier_id_pharmacy_suppliers
      FOREIGN KEY (supplier_id)
      REFERENCES pharmacy_suppliers (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_inventory'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_inventory
      ADD CONSTRAINT fk_pharmacy_inventory_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_inventory'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_inventory
      ADD CONSTRAINT fk_pharmacy_inventory_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_inventory_forecasts'
      AND rt.relname = 'pharmacy_inventory'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['inventory_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_inventory_forecasts
      ADD CONSTRAINT fk_pharmacy_inventory_forecasts_inventory_id_p_a76ae3a2
      FOREIGN KEY (inventory_id)
      REFERENCES pharmacy_inventory (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_inventory_forecasts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['generated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_inventory_forecasts
      ADD CONSTRAINT fk_pharmacy_inventory_forecasts_generated_by_users
      FOREIGN KEY (generated_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_pricing_rules'
      AND rt.relname = 'drugs'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['drug_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_pricing_rules
      ADD CONSTRAINT fk_pharmacy_pricing_rules_drug_id_drugs
      FOREIGN KEY (drug_id)
      REFERENCES drugs (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_pricing_rules'
      AND rt.relname = 'pharmacy_suppliers'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['supplier_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_pricing_rules
      ADD CONSTRAINT fk_pharmacy_pricing_rules_supplier_id_pharmacy_057f9218
      FOREIGN KEY (supplier_id)
      REFERENCES pharmacy_suppliers (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_pricing_rules'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_pricing_rules
      ADD CONSTRAINT fk_pharmacy_pricing_rules_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_pricing_rules'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_pricing_rules
      ADD CONSTRAINT fk_pharmacy_pricing_rules_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_purchase_order_items'
      AND rt.relname = 'pharmacy_purchase_orders'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['purchase_order_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_purchase_order_items
      ADD CONSTRAINT fk_pharmacy_purchase_order_items_purchase_orde_8ea53a1b
      FOREIGN KEY (purchase_order_id)
      REFERENCES pharmacy_purchase_orders (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_purchase_order_items'
      AND rt.relname = 'drugs'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['drug_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_purchase_order_items
      ADD CONSTRAINT fk_pharmacy_purchase_order_items_drug_id_drugs
      FOREIGN KEY (drug_id)
      REFERENCES drugs (id) ON DELETE RESTRICT;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_purchase_orders'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['order_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_pharmacy_purchase_orders_order_number
      ON pharmacy_purchase_orders (order_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_purchase_orders'
      AND rt.relname = 'pharmacy_suppliers'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['supplier_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_purchase_orders
      ADD CONSTRAINT fk_pharmacy_purchase_orders_supplier_id_pharma_effe2a36
      FOREIGN KEY (supplier_id)
      REFERENCES pharmacy_suppliers (id) ON DELETE RESTRICT;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_purchase_orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['approved_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_purchase_orders
      ADD CONSTRAINT fk_pharmacy_purchase_orders_approved_by_users
      FOREIGN KEY (approved_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_purchase_orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['ordered_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_purchase_orders
      ADD CONSTRAINT fk_pharmacy_purchase_orders_ordered_by_users
      FOREIGN KEY (ordered_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_purchase_orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_purchase_orders
      ADD CONSTRAINT fk_pharmacy_purchase_orders_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_purchase_orders'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_purchase_orders
      ADD CONSTRAINT fk_pharmacy_purchase_orders_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipt_items'
      AND rt.relname = 'pharmacy_receipts'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['receipt_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_receipt_items
      ADD CONSTRAINT fk_pharmacy_receipt_items_receipt_id_pharmacy_receipts
      FOREIGN KEY (receipt_id)
      REFERENCES pharmacy_receipts (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipt_items'
      AND rt.relname = 'pharmacy_purchase_order_items'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['purchase_order_item_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_receipt_items
      ADD CONSTRAINT fk_pharmacy_receipt_items_purchase_order_item__084c2aa9
      FOREIGN KEY (purchase_order_item_id)
      REFERENCES pharmacy_purchase_order_items (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipt_items'
      AND rt.relname = 'drugs'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['drug_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_receipt_items
      ADD CONSTRAINT fk_pharmacy_receipt_items_drug_id_drugs
      FOREIGN KEY (drug_id)
      REFERENCES drugs (id) ON DELETE RESTRICT;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipts'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['receipt_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_pharmacy_receipts_receipt_number
      ON pharmacy_receipts (receipt_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipts'
      AND rt.relname = 'pharmacy_purchase_orders'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['purchase_order_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_receipts
      ADD CONSTRAINT fk_pharmacy_receipts_purchase_order_id_pharmac_21378697
      FOREIGN KEY (purchase_order_id)
      REFERENCES pharmacy_purchase_orders (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipts'
      AND rt.relname = 'pharmacy_suppliers'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['supplier_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_receipts
      ADD CONSTRAINT fk_pharmacy_receipts_supplier_id_pharmacy_suppliers
      FOREIGN KEY (supplier_id)
      REFERENCES pharmacy_suppliers (id) ON DELETE RESTRICT;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['received_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_receipts
      ADD CONSTRAINT fk_pharmacy_receipts_received_by_users
      FOREIGN KEY (received_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['verified_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_receipts
      ADD CONSTRAINT fk_pharmacy_receipts_verified_by_users
      FOREIGN KEY (verified_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_receipts
      ADD CONSTRAINT fk_pharmacy_receipts_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_receipts'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_receipts
      ADD CONSTRAINT fk_pharmacy_receipts_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_return_items'
      AND rt.relname = 'pharmacy_returns'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['return_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_return_items
      ADD CONSTRAINT fk_pharmacy_return_items_return_id_pharmacy_returns
      FOREIGN KEY (return_id)
      REFERENCES pharmacy_returns (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_return_items'
      AND rt.relname = 'pharmacy_dispensing_items'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['dispensing_item_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_return_items
      ADD CONSTRAINT fk_pharmacy_return_items_dispensing_item_id_ph_c2c1d983
      FOREIGN KEY (dispensing_item_id)
      REFERENCES pharmacy_dispensing_items (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_return_items'
      AND rt.relname = 'pharmacy_inventory'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['inventory_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_return_items
      ADD CONSTRAINT fk_pharmacy_return_items_inventory_id_pharmacy_5d4472f1
      FOREIGN KEY (inventory_id)
      REFERENCES pharmacy_inventory (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_returns'
      AND rt.relname = 'pharmacy_dispensings'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['dispensing_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_returns
      ADD CONSTRAINT fk_pharmacy_returns_dispensing_id_pharmacy_dispensings
      FOREIGN KEY (dispensing_id)
      REFERENCES pharmacy_dispensings (id) ON DELETE RESTRICT;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_returns'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['returned_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_returns
      ADD CONSTRAINT fk_pharmacy_returns_returned_by_users
      FOREIGN KEY (returned_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_returns'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['approved_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_returns
      ADD CONSTRAINT fk_pharmacy_returns_approved_by_users
      FOREIGN KEY (approved_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_returns'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_returns
      ADD CONSTRAINT fk_pharmacy_returns_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_returns'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_returns
      ADD CONSTRAINT fk_pharmacy_returns_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_stock_adjustment_items'
      AND rt.relname = 'pharmacy_stock_adjustments'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['adjustment_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_stock_adjustment_items
      ADD CONSTRAINT fk_pharmacy_stock_adjustment_items_adjustment__58089f77
      FOREIGN KEY (adjustment_id)
      REFERENCES pharmacy_stock_adjustments (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_stock_adjustment_items'
      AND rt.relname = 'pharmacy_inventory'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['inventory_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_stock_adjustment_items
      ADD CONSTRAINT fk_pharmacy_stock_adjustment_items_inventory_i_4194cc80
      FOREIGN KEY (inventory_id)
      REFERENCES pharmacy_inventory (id) ON DELETE RESTRICT;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_stock_adjustments'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['adjustment_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_pharmacy_stock_adjustments_adjustment_number
      ON pharmacy_stock_adjustments (adjustment_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_stock_adjustments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['approved_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_stock_adjustments
      ADD CONSTRAINT fk_pharmacy_stock_adjustments_approved_by_users
      FOREIGN KEY (approved_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_stock_adjustments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['performed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_stock_adjustments
      ADD CONSTRAINT fk_pharmacy_stock_adjustments_performed_by_users
      FOREIGN KEY (performed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_stock_adjustments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_stock_adjustments
      ADD CONSTRAINT fk_pharmacy_stock_adjustments_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_stock_adjustments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_stock_adjustments
      ADD CONSTRAINT fk_pharmacy_stock_adjustments_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_stock_movements'
      AND rt.relname = 'pharmacy_inventory'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['inventory_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_stock_movements
      ADD CONSTRAINT fk_pharmacy_stock_movements_inventory_id_pharm_eafac684
      FOREIGN KEY (inventory_id)
      REFERENCES pharmacy_inventory (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_stock_movements'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['performed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_stock_movements
      ADD CONSTRAINT fk_pharmacy_stock_movements_performed_by_users
      FOREIGN KEY (performed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_substitution_recommendations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['review_id', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_pharmacy_sub_recommendations_review
      ON pharmacy_substitution_recommendations (review_id, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_substitution_recommendations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'recommendation_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_pharmacy_sub_recommendations_patient_status
      ON pharmacy_substitution_recommendations (patient_id, recommendation_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_suppliers'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_suppliers
      ADD CONSTRAINT fk_pharmacy_suppliers_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pharmacy_suppliers'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pharmacy_suppliers
      ADD CONSTRAINT fk_pharmacy_suppliers_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_action_executions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_action_executions_session_id
      ON post_visit_action_executions (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_action_executions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['recommendation_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_action_executions_recommendation_id
      ON post_visit_action_executions (recommendation_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_companion_acknowledgements'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_companion_acknowledgements_session_id
      ON post_visit_companion_acknowledgements (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_companion_acknowledgements'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_companion_acknowledgements_patient_id
      ON post_visit_companion_acknowledgements (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_companion_messages'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['thread_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_companion_messages_thread_id
      ON post_visit_companion_messages (thread_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_companion_messages'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_companion_messages_session_id
      ON post_visit_companion_messages (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_companion_messages'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_companion_messages_patient_id
      ON post_visit_companion_messages (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_companion_threads'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_companion_threads_session_id
      ON post_visit_companion_threads (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_companion_threads'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_companion_threads_patient_id
      ON post_visit_companion_threads (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_draft_artifacts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_draft_artifacts_session_id
      ON post_visit_draft_artifacts (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_draft_artifacts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['artifact_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_draft_artifacts_artifact_type
      ON post_visit_draft_artifacts (artifact_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_escalation_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_escalation_events_session_id
      ON post_visit_escalation_events (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_escalation_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_escalation_events_patient_id
      ON post_visit_escalation_events (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_escalation_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_escalation_events_status
      ON post_visit_escalation_events (status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_escalation_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['severity']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_escalation_events_severity
      ON post_visit_escalation_events (severity);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_escalation_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['route_target']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_escalation_events_route_target
      ON post_visit_escalation_events (route_target);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_extracted_entities'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_extracted_entities_session_id
      ON post_visit_extracted_entities (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_extracted_entities'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['entity_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_extracted_entities_entity_type
      ON post_visit_extracted_entities (entity_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_review_actions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_review_actions_session_id
      ON post_visit_review_actions (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_review_actions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['artifact_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_review_actions_artifact_type
      ON post_visit_review_actions (artifact_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_rule_citations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_rule_citations_session_id
      ON post_visit_rule_citations (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_rule_citations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['rule_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_rule_citations_rule_id
      ON post_visit_rule_citations (rule_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_rule_citations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['guideline_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_rule_citations_guideline_id
      ON post_visit_rule_citations (guideline_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tenant_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_sessions_tenant_id
      ON post_visit_sessions (tenant_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_sessions_patient_id
      ON post_visit_sessions (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['doctor_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_sessions_doctor_id
      ON post_visit_sessions (doctor_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_sessions_status
      ON post_visit_sessions (status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'post_visit_transcript_segments'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_post_visit_transcript_segments_session_id
      ON post_visit_transcript_segments (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pre_anesthesia_assessments'
      AND rt.relname = 'surgical_cases'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['surgical_case_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pre_anesthesia_assessments
      ADD CONSTRAINT fk_pre_anesthesia_assessments_surgical_case_id_69210cb0
      FOREIGN KEY (surgical_case_id)
      REFERENCES surgical_cases (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pre_anesthesia_assessments'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pre_anesthesia_assessments
      ADD CONSTRAINT fk_pre_anesthesia_assessments_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pre_anesthesia_assessments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['consent_obtained_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pre_anesthesia_assessments
      ADD CONSTRAINT fk_pre_anesthesia_assessments_consent_obtained_by_users
      FOREIGN KEY (consent_obtained_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'pre_anesthesia_assessments'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['assessed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE pre_anesthesia_assessments
      ADD CONSTRAINT fk_pre_anesthesia_assessments_assessed_by_users
      FOREIGN KEY (assessed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'prescription_templates'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE prescription_templates
      ADD CONSTRAINT fk_prescription_templates_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'prescriptions'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE prescriptions
      ADD CONSTRAINT fk_prescriptions_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'prescriptions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['doctor_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE prescriptions
      ADD CONSTRAINT fk_prescriptions_doctor_id_users
      FOREIGN KEY (doctor_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'prescriptions'
      AND rt.relname = 'medical_records'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['medical_record_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE prescriptions
      ADD CONSTRAINT fk_prescriptions_medical_record_id_medical_records
      FOREIGN KEY (medical_record_id)
      REFERENCES medical_records (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'preventive_care_reminders'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_preventive_care_reminders_patient_id
      ON preventive_care_reminders (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'prior_authorization_drafts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['claim_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_prior_authorization_drafts_claim_id
      ON prior_authorization_drafts (claim_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'prior_authorization_drafts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_prior_authorization_drafts_patient_id
      ON prior_authorization_drafts (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'prior_authorization_drafts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_prior_authorization_drafts_status
      ON prior_authorization_drafts (status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'prior_authorizations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_prior_authorizations_patient_id
      ON prior_authorizations (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'prior_authorizations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_prior_authorizations_status
      ON prior_authorizations (status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'proactive_alerts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_proactive_alerts_patient_id_status
      ON proactive_alerts (patient_id, status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'proactive_alerts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tenant_id', 'status', 'severity']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_proactive_alerts_tenant_id_status_severity
      ON proactive_alerts (tenant_id, status, severity);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'proactive_alerts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['target_user_id', 'status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_proactive_alerts_target_user_id_status
      ON proactive_alerts (target_user_id, status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'proactive_alerts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['dedup_key']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_proactive_alerts_dedup_key
      ON proactive_alerts (dedup_key);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'problems'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE problems
      ADD CONSTRAINT fk_problems_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'radiology_discrepancy_reviews'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['imaging_report_id', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_radiology_discrepancy_reviews_report_created
      ON radiology_discrepancy_reviews (imaging_report_id, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'radiology_discrepancy_reviews'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['imaging_study_id', 'discrepancy_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_radiology_discrepancy_reviews_study_status
      ON radiology_discrepancy_reviews (imaging_study_id, discrepancy_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'radiology_report_drafts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['imaging_study_id', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_radiology_report_drafts_study_created
      ON radiology_report_drafts (imaging_study_id, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'radiology_report_drafts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['linked_report_id', 'draft_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_radiology_report_drafts_report_status
      ON radiology_report_drafts (linked_report_id, draft_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'registration_ai_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_registration_ai_sessions_patient_id
      ON registration_ai_sessions (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'registration_ai_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_token']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_registration_ai_sessions_session_token
      ON registration_ai_sessions (session_token);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_monitoring_alerts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['event_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_remote_monitoring_alerts_event
      ON remote_monitoring_alerts (event_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_monitoring_alerts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_remote_monitoring_alerts_patient_status
      ON remote_monitoring_alerts (patient_id, status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_monitoring_alerts'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['severity']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_remote_monitoring_alerts_severity
      ON remote_monitoring_alerts (severity);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_monitoring_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_remote_monitoring_events_patient
      ON remote_monitoring_events (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_monitoring_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['device_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_remote_monitoring_events_device
      ON remote_monitoring_events (device_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_monitoring_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['source_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_remote_monitoring_events_source_type
      ON remote_monitoring_events (source_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_monitoring_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['verification_status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_remote_monitoring_events_verification_status
      ON remote_monitoring_events (verification_status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_monitoring_events'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['submitted_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_remote_monitoring_events_submitted_at
      ON remote_monitoring_events (submitted_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_patient_monitoring'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE remote_patient_monitoring
      ADD CONSTRAINT fk_remote_patient_monitoring_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'remote_patient_monitoring'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['uploaded_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE remote_patient_monitoring
      ADD CONSTRAINT fk_remote_patient_monitoring_uploaded_by_users
      FOREIGN KEY (uploaded_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_executions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['report_template_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_report_executions_template_id
      ON report_executions (report_template_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_executions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['scheduled_report_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_report_executions_scheduled_id
      ON report_executions (scheduled_report_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_executions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['executed_by']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_report_executions_executed_by
      ON report_executions (executed_by);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_executions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['execution_time']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_report_executions_execution_time
      ON report_executions (execution_time);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_executions'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_report_executions_status
      ON report_executions (status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_executions'
      AND rt.relname = 'report_templates'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['report_template_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE report_executions
      ADD CONSTRAINT fk_report_executions_report_template_id_report_c801f211
      FOREIGN KEY (report_template_id)
      REFERENCES report_templates (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_executions'
      AND rt.relname = 'scheduled_reports'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['scheduled_report_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE report_executions
      ADD CONSTRAINT fk_report_executions_scheduled_report_id_sched_d160dedb
      FOREIGN KEY (scheduled_report_id)
      REFERENCES scheduled_reports (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_executions'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['executed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE report_executions
      ADD CONSTRAINT fk_report_executions_executed_by_users
      FOREIGN KEY (executed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_favorites'
      AND c.contype = 'u'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['user_id', 'report_template_id']::text[]
  ) AND NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_favorites'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['user_id', 'report_template_id']::text[]
  ) THEN
    ALTER TABLE report_favorites
      ADD CONSTRAINT uq_report_favorites_user_id_report_template_id
      UNIQUE (user_id, report_template_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_favorites'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['user_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE report_favorites
      ADD CONSTRAINT fk_report_favorites_user_id_users
      FOREIGN KEY (user_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_favorites'
      AND rt.relname = 'report_templates'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['report_template_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE report_favorites
      ADD CONSTRAINT fk_report_favorites_report_template_id_report_templates
      FOREIGN KEY (report_template_id)
      REFERENCES report_templates (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_templates'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['report_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_report_templates_report_type
      ON report_templates (report_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_templates'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['category']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_report_templates_category
      ON report_templates (category);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_templates'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_report_templates_created_by
      ON report_templates (created_by);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'report_templates'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE report_templates
      ADD CONSTRAINT fk_report_templates_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'result_followup_tasks'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['encounter_copilot_session_id', 'status', 'created_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_result_followup_tasks_session_status
      ON result_followup_tasks (encounter_copilot_session_id, status, created_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'result_followup_tasks'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'priority', 'due_at']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_result_followup_tasks_patient_priority
      ON result_followup_tasks (patient_id, priority, due_at);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'result_followup_tasks'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['source_type', 'source_reference_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_result_followup_tasks_source
      ON result_followup_tasks (source_type, source_reference_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'scheduled_reports'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['template_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_scheduled_reports_template_id
      ON scheduled_reports (template_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'scheduled_reports'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['is_active']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_scheduled_reports_is_active
      ON scheduled_reports (is_active);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'scheduled_reports'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['next_run']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_scheduled_reports_next_run
      ON scheduled_reports (next_run);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'scheduled_reports'
      AND rt.relname = 'report_templates'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['template_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE scheduled_reports
      ADD CONSTRAINT fk_scheduled_reports_template_id_report_templates
      FOREIGN KEY (template_id)
      REFERENCES report_templates (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'scheduled_reports'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE scheduled_reports
      ADD CONSTRAINT fk_scheduled_reports_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'school_health_records'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE school_health_records
      ADD CONSTRAINT fk_school_health_records_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'school_health_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['assessed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE school_health_records
      ADD CONSTRAINT fk_school_health_records_assessed_by_users
      FOREIGN KEY (assessed_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'sdg_indicator_targets'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['indicator_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_sdg_indicator_targets_indicator_code
      ON sdg_indicator_targets (indicator_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'smartcare_patient_links'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['smartcare_patient_uuid']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_smartcare_patient_links_smartcare_patient_uuid
      ON smartcare_patient_links (smartcare_patient_uuid);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'sms_gateway_configurations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['provider_type']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_sms_gateway_configurations_provider_type
      ON sms_gateway_configurations (provider_type);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'sms_gateway_configurations'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['is_active']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_sms_gateway_configurations_is_active
      ON sms_gateway_configurations (is_active);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'social_determinants'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_social_determinants_patient_id
      ON social_determinants (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'superbill_templates'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_superbill_templates_created_by
      ON superbill_templates (created_by);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_cases'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['case_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_surgical_cases_case_number
      ON surgical_cases (case_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_cases'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_cases
      ADD CONSTRAINT fk_surgical_cases_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_cases'
      AND rt.relname = 'admissions'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['admission_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_cases
      ADD CONSTRAINT fk_surgical_cases_admission_id_admissions
      FOREIGN KEY (admission_id)
      REFERENCES admissions (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_cases'
      AND rt.relname = 'operating_rooms'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['operating_room_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_cases
      ADD CONSTRAINT fk_surgical_cases_operating_room_id_operating_rooms
      FOREIGN KEY (operating_room_id)
      REFERENCES operating_rooms (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_cases'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['primary_surgeon_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_cases
      ADD CONSTRAINT fk_surgical_cases_primary_surgeon_id_users
      FOREIGN KEY (primary_surgeon_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_cases'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['assistant_surgeon_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_cases
      ADD CONSTRAINT fk_surgical_cases_assistant_surgeon_id_users
      FOREIGN KEY (assistant_surgeon_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_cases'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['anesthesiologist_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_cases
      ADD CONSTRAINT fk_surgical_cases_anesthesiologist_id_users
      FOREIGN KEY (anesthesiologist_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_cases'
      AND rt.relname = 'patient_consents'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['consent_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_cases
      ADD CONSTRAINT fk_surgical_cases_consent_id_patient_consents
      FOREIGN KEY (consent_id)
      REFERENCES patient_consents (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_implants'
      AND rt.relname = 'surgical_cases'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['surgical_case_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_implants
      ADD CONSTRAINT fk_surgical_implants_surgical_case_id_surgical_cases
      FOREIGN KEY (surgical_case_id)
      REFERENCES surgical_cases (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_implants'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['implanted_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_implants
      ADD CONSTRAINT fk_surgical_implants_implanted_by_users
      FOREIGN KEY (implanted_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'surgical_preference_cards'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['surgeon_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE surgical_preference_cards
      ADD CONSTRAINT fk_surgical_preference_cards_surgeon_id_users
      FOREIGN KEY (surgeon_id)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_contact_investigations'
      AND rt.relname = 'tb_patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tb_patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_contact_investigations
      ADD CONSTRAINT fk_tb_contact_investigations_tb_patient_id_tb_patients
      FOREIGN KEY (tb_patient_id)
      REFERENCES tb_patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_contact_investigations'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['contact_patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_contact_investigations
      ADD CONSTRAINT fk_tb_contact_investigations_contact_patient_i_5addacd7
      FOREIGN KEY (contact_patient_id)
      REFERENCES patients (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_diagnoses'
      AND rt.relname = 'tb_patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tb_patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_diagnoses
      ADD CONSTRAINT fk_tb_diagnoses_tb_patient_id_tb_patients
      FOREIGN KEY (tb_patient_id)
      REFERENCES tb_patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_diagnoses'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_diagnoses
      ADD CONSTRAINT fk_tb_diagnoses_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_diagnoses'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['reported_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_diagnoses
      ADD CONSTRAINT fk_tb_diagnoses_reported_by_users
      FOREIGN KEY (reported_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_dot_records'
      AND rt.relname = 'tb_patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tb_patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_dot_records
      ADD CONSTRAINT fk_tb_dot_records_tb_patient_id_tb_patients
      FOREIGN KEY (tb_patient_id)
      REFERENCES tb_patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_dot_records'
      AND rt.relname = 'tb_treatment_episodes'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['episode_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_dot_records
      ADD CONSTRAINT fk_tb_dot_records_episode_id_tb_treatment_episodes
      FOREIGN KEY (episode_id)
      REFERENCES tb_treatment_episodes (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_dot_records'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_dot_records
      ADD CONSTRAINT fk_tb_dot_records_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_dot_records'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['dot_worker_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_dot_records
      ADD CONSTRAINT fk_tb_dot_records_dot_worker_id_users
      FOREIGN KEY (dot_worker_id)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_drug_susceptibilities'
      AND rt.relname = 'tb_patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tb_patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_drug_susceptibilities
      ADD CONSTRAINT fk_tb_drug_susceptibilities_tb_patient_id_tb_patients
      FOREIGN KEY (tb_patient_id)
      REFERENCES tb_patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_drug_susceptibilities'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_drug_susceptibilities
      ADD CONSTRAINT fk_tb_drug_susceptibilities_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_outcomes'
      AND rt.relname = 'tb_patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tb_patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_outcomes
      ADD CONSTRAINT fk_tb_outcomes_tb_patient_id_tb_patients
      FOREIGN KEY (tb_patient_id)
      REFERENCES tb_patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_outcomes'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_outcomes
      ADD CONSTRAINT fk_tb_outcomes_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_outcomes'
      AND rt.relname = 'tb_treatment_episodes'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['episode_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_outcomes
      ADD CONSTRAINT fk_tb_outcomes_episode_id_tb_treatment_episodes
      FOREIGN KEY (episode_id)
      REFERENCES tb_treatment_episodes (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_outcomes'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['recorded_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_outcomes
      ADD CONSTRAINT fk_tb_outcomes_recorded_by_users
      FOREIGN KEY (recorded_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_patients'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_patients
      ADD CONSTRAINT fk_tb_patients_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_patients'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['case_officer_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_patients
      ADD CONSTRAINT fk_tb_patients_case_officer_id_users
      FOREIGN KEY (case_officer_id)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_treatment_episodes'
      AND rt.relname = 'tb_patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tb_patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_treatment_episodes
      ADD CONSTRAINT fk_tb_treatment_episodes_tb_patient_id_tb_patients
      FOREIGN KEY (tb_patient_id)
      REFERENCES tb_patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_treatment_episodes'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_treatment_episodes
      ADD CONSTRAINT fk_tb_treatment_episodes_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'tb_treatment_episodes'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['prescribed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE tb_treatment_episodes
      ADD CONSTRAINT fk_tb_treatment_episodes_prescribed_by_users
      FOREIGN KEY (prescribed_by)
      REFERENCES users (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'tba_register'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['tba_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_tba_register_tba_code
      ON tba_register (tba_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_consents'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_consents
      ADD CONSTRAINT fk_telemedicine_consents_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_consents'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['witnessed_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_consents
      ADD CONSTRAINT fk_telemedicine_consents_witnessed_by_users
      FOREIGN KEY (witnessed_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_consultations'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['meeting_room_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_telemedicine_consultations_meeting_room_id
      ON telemedicine_consultations (meeting_room_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_consultations'
      AND rt.relname = 'appointments'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['appointment_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_consultations
      ADD CONSTRAINT fk_telemedicine_consultations_appointment_id_a_804388b3
      FOREIGN KEY (appointment_id)
      REFERENCES appointments (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_consultations'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_consultations
      ADD CONSTRAINT fk_telemedicine_consultations_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_consultations'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['doctor_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_consultations
      ADD CONSTRAINT fk_telemedicine_consultations_doctor_id_users
      FOREIGN KEY (doctor_id)
      REFERENCES users (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_consultations'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['created_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_consultations
      ADD CONSTRAINT fk_telemedicine_consultations_created_by_users
      FOREIGN KEY (created_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_consultations'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['updated_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_consultations
      ADD CONSTRAINT fk_telemedicine_consultations_updated_by_users
      FOREIGN KEY (updated_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_devices'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_devices
      ADD CONSTRAINT fk_telemedicine_devices_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_prescriptions'
      AND rt.relname = 'telemedicine_consultations'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['consultation_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_prescriptions
      ADD CONSTRAINT fk_telemedicine_prescriptions_consultation_id__9105e500
      FOREIGN KEY (consultation_id)
      REFERENCES telemedicine_consultations (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_prescriptions'
      AND rt.relname = 'prescriptions'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['prescription_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_prescriptions
      ADD CONSTRAINT fk_telemedicine_prescriptions_prescription_id__89d2807c
      FOREIGN KEY (prescription_id)
      REFERENCES prescriptions (id) ON DELETE SET NULL;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_technical_logs'
      AND rt.relname = 'telemedicine_consultations'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['consultation_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_technical_logs
      ADD CONSTRAINT fk_telemedicine_technical_logs_consultation_id_ef177986
      FOREIGN KEY (consultation_id)
      REFERENCES telemedicine_consultations (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'telemedicine_technical_logs'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['resolved_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE telemedicine_technical_logs
      ADD CONSTRAINT fk_telemedicine_technical_logs_resolved_by_users
      FOREIGN KEY (resolved_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'travel_vaccine_destinations'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['iso_code']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_travel_vaccine_destinations_iso_code
      ON travel_vaccine_destinations (iso_code);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'treatment_pathway_instances'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['encounter_copilot_session_id', 'recommendation_rank']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_treatment_pathway_instances_session_rank
      ON treatment_pathway_instances (encounter_copilot_session_id, recommendation_rank);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'treatment_pathway_instances'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id', 'status']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_treatment_pathway_instances_patient_status
      ON treatment_pathway_instances (patient_id, status);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'treatment_pathway_instances'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['pathway_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_treatment_pathway_instances_pathway
      ON treatment_pathway_instances (pathway_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'user_language_preferences'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['user_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_user_language_preferences_user_id
      ON user_language_preferences (user_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'users'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['email']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_users_email
      ON users (email);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'ussd_sessions'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['session_id']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_ussd_sessions_session_id
      ON ussd_sessions (session_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'vaccination_certificates'
      AND i.indpred IS NULL
      AND i.indisunique = FALSE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
  ) THEN
    CREATE INDEX IF NOT EXISTS idx_vaccination_certificates_patient_id
      ON vaccination_certificates (patient_id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_index i
    JOIN pg_class t ON t.oid = i.indrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    WHERE ns.nspname = 'public'
      AND t.relname = 'vaccination_certificates'
      AND i.indpred IS NULL
      AND i.indisunique = TRUE
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(i.indkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['certificate_number']::text[]
  ) THEN
    CREATE UNIQUE INDEX IF NOT EXISTS uidx_vaccination_certificates_certificate_number
      ON vaccination_certificates (certificate_number);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'vhf_contacts'
      AND rt.relname = 'vhf_cases'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['case_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE vhf_contacts
      ADD CONSTRAINT fk_vhf_contacts_case_id_vhf_cases
      FOREIGN KEY (case_id)
      REFERENCES vhf_cases (id) ON DELETE CASCADE;
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'vitals'
      AND rt.relname = 'patients'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['patient_id']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE vitals
      ADD CONSTRAINT fk_vitals_patient_id_patients
      FOREIGN KEY (patient_id)
      REFERENCES patients (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
  `DO $$ BEGIN
  IF NOT EXISTS (
    SELECT 1
    FROM pg_constraint c
    JOIN pg_class t ON t.oid = c.conrelid
    JOIN pg_namespace ns ON ns.oid = t.relnamespace
    JOIN pg_class rt ON rt.oid = c.confrelid
    WHERE ns.nspname = 'public'
      AND t.relname = 'vitals'
      AND rt.relname = 'users'
      AND c.contype = 'f'
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.conkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = t.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['recorded_by']::text[]
      AND (
        SELECT array_agg(att.attname::text ORDER BY cols.ord)
        FROM unnest(c.confkey) WITH ORDINALITY cols(attnum, ord)
        JOIN pg_attribute att
          ON att.attrelid = rt.oid
         AND att.attnum = cols.attnum
      ) = ARRAY['id']::text[]
  ) THEN
    ALTER TABLE vitals
      ADD CONSTRAINT fk_vitals_recorded_by_users
      FOREIGN KEY (recorded_by)
      REFERENCES users (id);
  END IF;
EXCEPTION WHEN others THEN NULL;
END $$;`,
];
