# MediCore Mobile — Environment Variables
# Copy this to .env and fill in values

# API base (no protocol — the code prepends https:// / wss://)
EXPO_PUBLIC_API_BASE=localhost:3000
EXPO_PUBLIC_WS_BASE=ws://localhost:3000
